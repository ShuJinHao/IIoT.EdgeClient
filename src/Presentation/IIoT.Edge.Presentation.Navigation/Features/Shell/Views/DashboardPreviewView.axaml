<UserControl
    x:Class="IIoT.Edge.Presentation.Navigation.Features.Shell.DashboardPreviewView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:edge="clr-namespace:IIoT.Edge.UI.Shared.Avalonia.Controls;assembly=IIoT.Edge.UI.Shared"
    xmlns:sharedViews="clr-namespace:IIoT.Edge.UI.Shared.Avalonia.Views;assembly=IIoT.Edge.UI.Shared">
    <Grid Margin="12,4,18,8" RowDefinitions="Auto,220,*" RowSpacing="8">
        <Grid>
            <StackPanel Spacing="2">
                <TextBlock
                    Classes="edge-dashboard-preview-title"
                    Text="{DynamicResource Navigation_DashboardPreview_Title}" />
                <TextBlock
                    Classes="edge-dashboard-preview-subtitle"
                    Text="{DynamicResource Navigation_DashboardPreview_Subtitle}" />
            </StackPanel>
        </Grid>

        <Grid
            Grid.Row="1"
            ColumnDefinitions="1.08*,0.82*,0.72*"
            ColumnSpacing="12">
            <edge:EdgeMetricCard
                Description="{Binding RecentHourDescription}"
                Icon="{StaticResource Edge.Icon.Dashboard}"
                MinHeight="0"
                Status="Info"
                Title="{DynamicResource Navigation_DashboardPreview_RecentHourOutput}"
                Unit="{DynamicResource Navigation_DashboardPreview_UnitPiece}"
                Value="{Binding RecentHourOutput}" />

            <edge:EdgeCard
                Grid.Column="1"
                Classes="summary"
                Elevation="Flat"
                PaddingMode="Compact"
                Surface="Card"
                MinHeight="0"
                >
                <Grid RowDefinitions="Auto,*" RowSpacing="{DynamicResource Edge.Size.Spacing10}">
                    <TextBlock
                        Classes="edge-summary-card-title"
                        Text="{DynamicResource Navigation_DashboardPreview_RecipeAndPlc}" />
                    <ItemsControl
                        Grid.Row="1"
                        Classes="edge-summary-card-items"
                        ItemsSource="{Binding ProductionSummaryItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Spacing="{DynamicResource Edge.Size.Spacing6}" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Classes="edge-summary-card-row">
                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{DynamicResource Edge.Size.Spacing12}">
                                        <TextBlock
                                            Classes="edge-summary-card-label"
                                            Text="{Binding Label}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            Classes="edge-summary-card-value"
                                            Text="{Binding Value}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </edge:EdgeCard>

            <edge:EdgeCard
                Grid.Column="2"
                Elevation="Flat"
                PaddingMode="Compact"
                Surface="Card">
                <Grid
                    ColumnDefinitions="*,*"
                    RowDefinitions="*,*"
                    ColumnSpacing="10"
                    RowSpacing="10">
                    <Grid>
                        <StackPanel Classes="edge-dashboard-mini-block">
                            <StackPanel Classes="edge-dashboard-mini-label-row">
                                <TextBlock
                                    Classes="edge-dashboard-mini-label"
                                    Text="{DynamicResource Navigation_DashboardPreview_DeviceLinks}" />
                                <edge:EdgeStatusDot VerticalAlignment="Center" Status="{Binding DeviceLinksStatus}" />
                            </StackPanel>
                            <TextBlock
                                Classes="edge-dashboard-mini-value"
                                Text="{Binding ConnectedDevices}" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1">
                        <StackPanel Classes="edge-dashboard-mini-block">
                            <StackPanel Classes="edge-dashboard-mini-label-row">
                                <TextBlock
                                    Classes="edge-dashboard-mini-label"
                                    Text="{DynamicResource Navigation_DashboardPreview_PlcAverageLatency}" />
                                <edge:EdgeStatusDot VerticalAlignment="Center" Status="{Binding PlcLatencyStatus}" />
                            </StackPanel>
                            <TextBlock
                                Classes="edge-dashboard-mini-value"
                                Text="{Binding PlcLatencyText}" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1">
                        <StackPanel Classes="edge-dashboard-mini-block">
                            <StackPanel Classes="edge-dashboard-mini-label-row">
                                <TextBlock
                                    Classes="edge-dashboard-mini-label"
                                    Text="{DynamicResource Navigation_DashboardPreview_Mes}" />
                                <edge:EdgeStatusDot VerticalAlignment="Center" Status="{Binding MesStatus}" />
                            </StackPanel>
                            <TextBlock
                                Classes="edge-dashboard-mini-value"
                                Text="{Binding MesStateText}" />
                            <TextBlock
                                Classes="edge-dashboard-mini-detail"
                                IsVisible="{Binding HasMesProbeText}"
                                Text="{Binding MesProbeText}" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1" Grid.Column="1">
                        <StackPanel Classes="edge-dashboard-mini-block">
                            <StackPanel Classes="edge-dashboard-mini-label-row">
                                <TextBlock
                                    Classes="edge-dashboard-mini-label"
                                    Text="{DynamicResource Navigation_DashboardPreview_Cloud}" />
                                <edge:EdgeStatusDot VerticalAlignment="Center" Status="{Binding CloudStatus}" />
                            </StackPanel>
                            <TextBlock
                                Classes="edge-dashboard-mini-value"
                                Text="{Binding CloudStateText}" />
                            <TextBlock
                                Classes="edge-dashboard-mini-detail"
                                IsVisible="{Binding HasCloudProbeText}"
                                Text="{Binding CloudProbeText}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </edge:EdgeCard>
        </Grid>

        <Grid
            Grid.Row="2"
            ColumnDefinitions="1.85*,*"
            ColumnSpacing="12">
            <edge:EdgeTablePanel
                Classes="fill"
                IsEmpty="{Binding IsPlcStatusTableEmpty}"
                EmptyMessage="{DynamicResource Navigation_DashboardPreview_NoPlcStatusSource}"
                EmptyTitle="{DynamicResource Navigation_DashboardPreview_NoPlcStatusTitle}"
                Title="{DynamicResource Navigation_DashboardPreview_PlcStatusTableTitle}">
                <edge:EdgeTablePanel.HeaderMetaContent>
                    <TextBlock Classes="edge-table-count">
                        <Run Text="{DynamicResource Navigation_Table_TotalPrefix}" />
                        <Run Text="{Binding PlcStatusTableItems.Count}" />
                        <Run Text="{DynamicResource Navigation_Table_TotalSuffix}" />
                    </TextBlock>
                </edge:EdgeTablePanel.HeaderMetaContent>

                <edge:EdgeDataGrid
                    x:Name="DashboardPreviewPlcStatusGrid"
                    AutoGenerateColumns="False"
                    CanUserReorderColumns="False"
                    CanUserResizeColumns="True"
                    CanUserSortColumns="False"
                    IsReadOnly="True"
                    ItemsSource="{Binding PlcStatusTableItems}"
                    ViewportMaxHeight="0">
                    <edge:EdgeDataGrid.Columns>
                        <edge:EdgeTextColumn
                            Binding="{Binding DeviceName}"
                            Header="{DynamicResource Navigation_DashboardPreview_PlcDevice}"
                            MinWidth="110"
                            Width="1.4*" />
                        <edge:EdgeTemplateColumn
                            Header="{DynamicResource Navigation_DashboardPreview_PlcState}"
                            MinWidth="136"
                            Width="1.2*">
                            <edge:EdgeTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel
                                        Classes="edge-dashboard-alert-level"
                                        VerticalAlignment="Center">
                                        <edge:EdgeStatusDot Status="{Binding Status}" />
                                        <TextBlock
                                            Classes="edge-dashboard-alert-level-text"
                                            Text="{Binding StateText}" />
                                    </StackPanel>
                                </DataTemplate>
                            </edge:EdgeTemplateColumn.CellTemplate>
                        </edge:EdgeTemplateColumn>
                        <edge:EdgeTextColumn
                            Binding="{Binding LatencyText}"
                            Header="{DynamicResource Navigation_DashboardPreview_PlcLatency}"
                            MinWidth="92"
                            Width="0.9*" />
                        <edge:EdgeTextColumn
                            Binding="{Binding LastConnectedText}"
                            Header="{DynamicResource Navigation_DashboardPreview_PlcLastConnected}"
                            MinWidth="132"
                            Width="1.1*" />
                        <edge:EdgeTextColumn
                            Binding="{Binding LastFailureText}"
                            Header="{DynamicResource Navigation_DashboardPreview_PlcLastFailure}"
                            MinWidth="132"
                            Width="1.1*" />
                        <edge:EdgeTemplateColumn
                            Header="{DynamicResource Navigation_DashboardPreview_PlcDetails}"
                            MinWidth="118"
                            Width="118">
                            <edge:EdgeTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <edge:EdgeActionButton
                                        Command="{Binding DataContext.ShowPlcStatusDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="{DynamicResource Navigation_DashboardPreview_PlcDetails}"
                                        Kind="Ghost"
                                        Role="Cell"
                                        Size="Compact"
                                        VerticalAlignment="Center" />
                                </DataTemplate>
                            </edge:EdgeTemplateColumn.CellTemplate>
                        </edge:EdgeTemplateColumn>
                    </edge:EdgeDataGrid.Columns>
                </edge:EdgeDataGrid>
            </edge:EdgeTablePanel>

            <edge:EdgeCard
                Grid.Column="1"
                Classes="dashboard-data-panel"
                MinHeight="0">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock
                        Classes="edge-dashboard-data-panel-title"
                        Text="{Binding UploadHealthTitle}" />
                    <Grid Grid.Row="1" Classes="edge-dashboard-data-panel-body">
                        <StackPanel
                            IsVisible="{Binding IsUploadHealthBodyVisible}"
                            Spacing="{DynamicResource Edge.Size.Spacing12}"
                            VerticalAlignment="Top">
                            <edge:EdgeStatusListItem
                                Classes="compact strip"
                                Status="{Binding UploadHealthStatus}"
                                Title="{Binding UploadHealthStatusText}" />

                            <ItemsControl ItemsSource="{Binding UploadChannelItems}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel
                                            Orientation="Horizontal"
                                            Spacing="{DynamicResource Edge.Size.Spacing8}" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <edge:EdgeCard
                                            MinWidth="132"
                                            Classes="summary"
                                            Elevation="Flat"
                                            PaddingMode="Compact"
                                            Surface="Wash">
                                            <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="{DynamicResource Edge.Size.Spacing4}">
                                                <StackPanel
                                                    Orientation="Horizontal"
                                                    Spacing="{DynamicResource Edge.Size.Spacing6}">
                                                    <edge:EdgeStatusDot
                                                        VerticalAlignment="Center"
                                                        Status="{Binding Status}" />
                                                    <TextBlock
                                                        Classes="edge-summary-card-title"
                                                        Text="{Binding Name}" />
                                                </StackPanel>
                                                <TextBlock
                                                    Grid.Row="1"
                                                    Classes="edge-summary-card-value"
                                                    Text="{Binding StateText}" />
                                                <TextBlock
                                                    Grid.Row="2"
                                                    Classes="edge-summary-card-label"
                                                    IsVisible="{Binding HasDetailText}"
                                                    Text="{Binding DetailText}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <ItemsControl
                                                    Grid.Row="3"
                                                    IsVisible="{Binding HasMetrics}"
                                                    ItemsSource="{Binding Metrics}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Spacing="{DynamicResource Edge.Size.Spacing2}" />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Classes="edge-summary-card-row">
                                                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                                                                    <TextBlock
                                                                        Classes="edge-summary-card-label"
                                                                        Text="{Binding Label}" />
                                                                    <TextBlock
                                                                        Grid.Column="1"
                                                                        Classes="edge-summary-card-value"
                                                                        Text="{Binding Value}" />
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </edge:EdgeCard>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <edge:EdgeStatusSegmentBar ItemsSource="{Binding UploadHealthSegments}" />

                            <edge:EdgeCard
                                Classes="summary"
                                Elevation="Flat"
                                PaddingMode="None"
                                Surface="Transparent">
                                <ItemsControl ItemsSource="{Binding UploadHealthSummaryItems}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel
                                                Orientation="Horizontal"
                                                Spacing="{DynamicResource Edge.Size.Spacing12}" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border
                                                MinWidth="96"
                                                Classes="edge-summary-card-row">
                                                <Grid RowDefinitions="Auto,Auto" RowSpacing="{DynamicResource Edge.Size.Spacing4}">
                                                    <TextBlock
                                                        Classes="edge-summary-card-label"
                                                        Text="{Binding Label}" />
                                                    <TextBlock
                                                        Grid.Row="1"
                                                        Classes="edge-summary-card-value"
                                                        Text="{Binding Value}" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </edge:EdgeCard>
                        </StackPanel>

                        <sharedViews:EmptyStateView
                            IsVisible="{Binding IsUploadHealthEmpty}"
                            Message="{Binding UploadHealthEmptyMessage}"
                            Title="{Binding UploadHealthEmptyTitle}" />
                    </Grid>
                </Grid>
            </edge:EdgeCard>
        </Grid>
        <Border
            Grid.RowSpan="3"
            Classes="edge-dialog-overlay"
            IsVisible="{Binding IsPlcStatusDetailOpen}">
            <edge:EdgeDialogChrome
                Classes="inline-dialog"
                CloseCommand="{Binding ClosePlcStatusDetailCommand}"
                CloseTopLevelOnClose="False"
                HorizontalAlignment="Center"
                MaxWidth="620"
                MinWidth="460"
                Title="{DynamicResource Navigation_DashboardPreview_PlcDetailTitle}"
                VerticalAlignment="Center">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,*" RowSpacing="{DynamicResource Edge.Size.Spacing8}">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcDevice}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.DeviceName}" />
                    </Grid>
                    <Grid
                        Grid.Row="1"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcState}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.StateText}" />
                    </Grid>
                    <Grid
                        Grid.Row="2"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcEndpoint}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.EndpointText}" />
                    </Grid>
                    <Grid
                        Grid.Row="3"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcDeviceModel}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.DeviceModelText}" />
                    </Grid>
                    <Grid
                        Grid.Row="4"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcProtocolFrame}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.ProtocolFrameText}" />
                    </Grid>
                    <Grid
                        Grid.Row="5"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcLastAttempt}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.LastAttemptText}" />
                    </Grid>
                    <Grid
                        Grid.Row="6"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcLastConnected}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.LastConnectedText}" />
                    </Grid>
                    <Grid
                        Grid.Row="7"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcLastRead}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.LastReadText}" />
                    </Grid>
                    <Grid
                        Grid.Row="8"
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="{DynamicResource Edge.Size.Spacing8}">
                        <TextBlock
                            Classes="edge-summary-card-label"
                            Text="{DynamicResource Navigation_DashboardPreview_PlcLastFailure}" />
                        <TextBlock
                            Grid.Column="1"
                            Classes="edge-summary-card-value"
                            Text="{Binding SelectedPlcStatusDetail.LastFailureText}" />
                    </Grid>
                    <TextBlock
                        Grid.Row="9"
                        Classes="edge-summary-card-label"
                        Text="{DynamicResource Navigation_DashboardPreview_PlcErrorDetail}" />
                    <edge:EdgeScrollHost
                        Grid.Row="10"
                        MaxHeight="220"
                        VerticalScrollBarVisibility="Auto">
                        <TextBlock
                            Text="{Binding SelectedPlcStatusDetail.LastErrorDetail}"
                            TextWrapping="Wrap" />
                    </edge:EdgeScrollHost>
                </Grid>
                <edge:EdgeDialogChrome.FooterContent>
                    <StackPanel Classes="edge-dialog-actions">
                        <edge:EdgeActionButton
                            Command="{Binding ClosePlcStatusDetailCommand}"
                            Content="{DynamicResource Navigation_DashboardPreview_Close}"
                            Kind="Secondary"
                            Size="Compact" />
                    </StackPanel>
                </edge:EdgeDialogChrome.FooterContent>
            </edge:EdgeDialogChrome>
        </Border>
    </Grid>
</UserControl>
