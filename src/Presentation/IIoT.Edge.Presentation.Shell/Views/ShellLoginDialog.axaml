<Window
    x:Class="IIoT.Edge.Presentation.Shell.Views.ShellLoginDialog"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:edge="clr-namespace:IIoT.Edge.UI.Shared.Avalonia.Controls;assembly=IIoT.Edge.UI.Shared"
    Width="960"
    Height="640"
    Background="Transparent"
    CanResize="False"
    ExtendClientAreaToDecorationsHint="True"
    ShowInTaskbar="False"
    Title="{DynamicResource Shell_Login_Title}"
    TransparencyLevelHint="Transparent"
    WindowDecorations="None"
    WindowStartupLocation="Manual">
    <Grid Classes="shell-login-overlay">
        <edge:EdgeCard
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Classes="shell-login-panel"
            Elevation="Float"
            PaddingMode="None"
            Surface="Card">
            <Grid RowDefinitions="Auto,Auto">
                <Grid
                    Grid.Row="0"
                    Classes="shell-login-header"
                    ColumnDefinitions="*,Auto">
                    <StackPanel
                        Grid.Column="0"
                        Spacing="4">
                        <TextBlock
                            Classes="shell-login-title"
                            Text="{DynamicResource Shell_Login_Title}" />
                        <TextBlock
                            Classes="shell-login-subtitle"
                            Text="{DynamicResource Shell_Login_Subtitle}" />
                    </StackPanel>

                    <edge:EdgeWindowButton
                        Grid.Column="1"
                        Action="Close"
                        Classes="shell-login-close"
                        Click="OnCancelClick" />
                </Grid>

                <Grid
                    Grid.Row="1"
                    Classes="shell-login-body"
                    RowDefinitions="Auto,18,Auto,12,Auto,18,Auto">
                    <edge:EdgeSegmentedNav
                        x:Name="LoginModeTabs"
                        Grid.Row="0"
                        Classes="shell-login-mode-nav"
                        HorizontalAlignment="Stretch"
                        SelectionChanged="OnModeSelectionChanged"
                        SelectedIndex="0">
                        <edge:EdgeSegmentedNavItem Content="{DynamicResource Shell_Login_LocalEmergency}" />
                        <edge:EdgeSegmentedNavItem Content="{DynamicResource Shell_Login_CloudEmployee}" />
                    </edge:EdgeSegmentedNav>

                    <Grid
                        x:Name="LocalForm"
                        Grid.Row="2"
                        RowDefinitions="Auto,10,Auto,10,Auto,10,Auto">
                        <TextBlock
                            x:Name="LocalDescriptionText"
                            Grid.Row="0"
                            Classes="shell-login-form-label"
                            Text="{DynamicResource Shell_Login_LocalDescription}" />

                        <Grid
                            x:Name="LocalLoginPanel"
                            Grid.Row="2"
                            RowDefinitions="Auto">
                            <edge:EdgeTextBox
                                x:Name="LocalPasswordInput"
                                Grid.Row="0"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_LocalPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                        </Grid>

                        <Grid
                            x:Name="LocalSetupPanel"
                            Grid.Row="2"
                            IsVisible="False"
                            RowDefinitions="Auto,10,Auto">
                            <edge:EdgeTextBox
                                x:Name="LocalSetupNewPasswordInput"
                                Grid.Row="0"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_NewPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                            <edge:EdgeTextBox
                                x:Name="LocalSetupConfirmPasswordInput"
                                Grid.Row="2"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_ConfirmPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                        </Grid>

                        <Grid
                            x:Name="LocalResetPanel"
                            Grid.Row="2"
                            IsVisible="False"
                            RowDefinitions="Auto,10,Auto,10,Auto">
                            <edge:EdgeTextBox
                                x:Name="LocalResetCurrentPasswordInput"
                                Grid.Row="0"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_CurrentPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                            <edge:EdgeTextBox
                                x:Name="LocalResetNewPasswordInput"
                                Grid.Row="2"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_NewPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                            <edge:EdgeTextBox
                                x:Name="LocalResetConfirmPasswordInput"
                                Grid.Row="4"
                                Classes="edge-text-box shell-login-input"
                                HorizontalAlignment="Left"
                                PasswordChar="*"
                                PlaceholderText="{DynamicResource Shell_Login_ConfirmPasswordHint}"
                                KeyDown="OnInputKeyDown" />
                        </Grid>
                    </Grid>

                    <Grid
                        x:Name="CloudForm"
                        Grid.Row="2"
                        IsVisible="False"
                        RowDefinitions="Auto,10,Auto,10,Auto,10,Auto">
                        <TextBlock
                            Grid.Row="0"
                            Classes="shell-login-form-label"
                            Text="{DynamicResource Shell_Login_CloudDescription}" />
                        <edge:EdgeTextBox
                            x:Name="EmployeeNoInput"
                            Grid.Row="2"
                            Classes="edge-text-box shell-login-input"
                            HorizontalAlignment="Left"
                            PlaceholderText="{DynamicResource Shell_Login_EmployeeNoHint}"
                            KeyDown="OnInputKeyDown" />
                        <edge:EdgeTextBox
                            x:Name="CloudPasswordInput"
                            Grid.Row="4"
                            Classes="edge-text-box shell-login-input"
                            HorizontalAlignment="Left"
                            PasswordChar="*"
                            PlaceholderText="{DynamicResource Shell_Login_PasswordHint}"
                            KeyDown="OnInputKeyDown" />
                        <Border
                            x:Name="CloudUnavailableNotice"
                            Grid.Row="6"
                            Classes="shell-login-notice"
                            IsVisible="False">
                            <TextBlock
                                Classes="shell-login-notice-text"
                                Text="{DynamicResource Shell_Login_CloudUnavailable}"
                                TextWrapping="Wrap" />
                        </Border>
                    </Grid>

                    <TextBlock
                        x:Name="ErrorText"
                        Grid.Row="4"
                        Classes="shell-login-error"
                        TextWrapping="Wrap" />

                    <StackPanel
                        Grid.Row="6"
                        Classes="shell-login-actions">
                        <edge:EdgeActionButton
                            Classes="shell-login-cancel"
                            Click="OnCancelClick"
                            Icon="{StaticResource Edge.Icon.Close}"
                            Kind="Secondary"
                            Size="Compact"
                            Content="{DynamicResource Shell_Login_Cancel}" />
                        <edge:EdgeActionButton
                            x:Name="LoginButton"
                            Classes="shell-login-submit"
                            Click="OnLoginClick"
                            Icon="{StaticResource Edge.Icon.Login}"
                            Kind="Primary"
                            Size="Compact"
                            Content="{DynamicResource Shell_Login_Submit}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </edge:EdgeCard>
    </Grid>
</Window>
