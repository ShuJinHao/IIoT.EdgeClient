# Avalonia 12 现场试运行验收记录模板

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 试运行日期 |  |
| 产线/工位 |  |
| 工控机编号 |  |
| 操作人 |  |
| 发布包路径 |  |
| release-manifest commit |  |
| 证据包路径 |  |

## 启动检查

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| UI-only 启动 | 通过 / 不通过 |  |
| `--start-runtime` 启动 | 通过 / 不通过 |  |
| WPF Launcher 回退 | 通过 / 不通过 |  |
| WPF Shell 回退 | 通过 / 不通过 |  |
| 启动失败详情可复制 | 通过 / 不通过 / 不适用 |  |

## UI 检查

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| Header/Footer 状态 | 通过 / 不通过 |  |
| 菜单与导航 | 通过 / 不通过 |  |
| Dock 拖拽与停靠 | 通过 / 不通过 |  |
| 语言切换 | 通过 / 不通过 |  |
| DataGrid 滚动与列头 | 通过 / 不通过 |  |
| 高 DPI / 多屏 / 触摸 | 通过 / 不通过 |  |

## 运行联调

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| IOView 只读快照 | 通过 / 不通过 |  |
| I/O 写入申请进入运行时缓冲 | 通过 / 不通过 / 未执行 |  |
| PLC 块写入轨迹可见 | 通过 / 不通过 / 未执行 |  |
| Equipment 最近 PLC 块写入状态 | 通过 / 不通过 / 未执行 |  |
| Diagnostics 现场摘要 | 通过 / 不通过 |  |
| Cloud/MES 只读状态 | 通过 / 不通过 |  |

## 证据留存

| 证据项 | 是否已留存 | 路径/说明 |
| --- | --- | --- |
| release-manifest.json | 是 / 否 |  |
| candidate-validation-summary.json | 是 / 否 |  |
| 现场证据包 zip | 是 / 否 |  |
| Diagnostics 现场摘要截图 | 是 / 否 |  |
| I/O 写入闸门截图 | 是 / 否 |  |
| PLC 写入轨迹截图 | 是 / 否 |  |
| WPF 回退截图 | 是 / 否 |  |

## 结论

| 结论项 | 选择 |
| --- | --- |
| P0 阻断项 | 无 / 有 |
| P1 阻断项 | 无 / 有 |
| 是否允许继续试运行 | 是 / 否 |
| 是否允许进入切默认入口评审 | 是 / 否 |

## 问题记录

| 等级 | 问题 | 影响 | 证据 | 处理建议 |
| --- | --- | --- | --- | --- |
| P0 / P1 / P2 |  |  |  |  |
