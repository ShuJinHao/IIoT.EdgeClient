# 客户端待后续处理问题

本文保留尚未满足、部分满足以及尚待用户确认才能移除的 `GAP-*`。B01 删除的 `业务规则.md` 已由 B02 原字节恢复；其中 62 条稳定规则仍是受保护的完整规则来源。B11-A/B 已将 62/62 条迁入 `../客户端规则.md`；正式规则另有独立新增的 `HL-RELEASE-001`，因此当前共有 63 个编号。迁移完成和原文删除是两项独立状态，用户确认删除前仍允许暂时重复。

## 1. 记录口径

- “当前源码表现”以各项所列当前源码及不可变合并证据为事实；处于源码候选状态的条目必须继续保留，不能替代真实 Windows、PLC 或用户确认。
- `未满足` 表示验收条件没有形成；`部分满足` 只列已经具备的源码事实和剩余条件，禁止把局部实现冒充整项完成。
- B16 功能实现基线为 Host `2.0.14` / Edge implementation merge `96be337928e2debb1f460baefe436a1e461b4f95`、SDK `2.0.11` / merge `fe8b4c46c48197eecbeaf74a8463cd347edf9129`、AP/CP `2.0.19` / Plugins merge `183adc65eb4299f216de136490285bdc482b03ab`。后续文档事实同步只推进控制面 SHA，不改变上述功能实现；B17 必须重新读取届时 clean `main` 并以实时 SHA 冻结 exact 候选，不得直接把 implementation merge 当作最终 Host SHA。本轮只形成源码与受影响测试证据，尚未运行 B17 exact-SHA、Validate、Prepare、catalog Deploy 或 Windows/真实 PLC 验收；Published 仍为 Host `2.0.11`、AP/CP `2.0.13`。
- B01 曾静默移除 7 个 GAP；B02 已全部恢复并重新裁决。后续任何 GAP 只有在源码、受影响测试、规则映射和用户确认均完成后才允许移除，不能只凭“已取代”或局部实现删除记录。
- 62 条稳定规则只有在 62/62 完整迁移、前后映射和引用扫描通过、恢复提交与 hash 可追溯、PR review 完成并取得用户明确确认后，才允许删除原 `业务规则.md`。
- 源码证据路径均相对于工作区根目录。

### 1.1 62 条业务规则迁移矩阵

- 受保护原文由 commit `15457cfed9e89b98123c6a3191cc2f39ea847d4b` 原字节恢复；当前固定为 712 行、53,376 字节、SHA-256 `63ee46c9f8880268be1b44ef64ab677577ee5d47e724af04995d8544aae3b764`。
- “迁移关系”只允许 `等价`、`澄清`、`受控替代`；后两者必须在本表说明不会把旧冲突语义重新带入正式规则。
- “当前实现状态”只允许 `源码满足`、`部分满足`、`未满足`、`现场待验收`。该列描述功能事实，不描述文档是否已经迁入。
- B11-A 迁入 Host/Launcher 9 条和 PLC 7 条，B11-B 迁入 Cloud/MES 21 条；加上此前 25 条后累计 62/62。文档迁移不改变“当前实现状态”，不得据本矩阵宣称功能、现场或生产已经完成。

| 序号 | 原规则 | 原文锚点 | 正式目标 | 迁移关系 | 当前实现状态 | 证据与关联 GAP |
|---:|---|---|---|---|---|---|
| 1 | `HL-ARCH-001` | `业务规则.md#HL-ARCH-001` | `客户端规则.md#HL-ARCH-001` | 等价 | 源码满足 | `客户端规则.md` 第 4、5 节；Host/SDK/Private Plugins 独立包与装载边界 |
| 2 | `HL-PROFILE-001` | `业务规则.md#HL-PROFILE-001` | `客户端规则.md#HL-PROFILE-001` | 等价 | 现场待验收 | `LauncherProfileCatalog`、`ShellLaunchService`；AP/CP Windows 并发未验收 |
| 3 | `HL-SOURCE-001` | `业务规则.md#HL-SOURCE-001` | `客户端规则.md#HL-SOURCE-001` | 等价 | 源码满足 | `GAP-HL-001/003`；Windows/用户确认未完成 |
| 4 | `HL-CLOUD-001` | `业务规则.md#HL-CLOUD-001` | `客户端规则.md#HL-CLOUD-001` | 澄清 | 部分满足 | 与 `HL-RELEASE-001` 分离业务配置和发布控制面；`GAP-HL-002/003` |
| 5 | `HL-META-001` | `业务规则.md#HL-META-001` | `客户端规则.md#HL-META-001` | 等价 | 部分满足 | `GAP-HL-002/004` |
| 6 | `HL-CONFIG-001` | `业务规则.md#HL-CONFIG-001` | `客户端规则.md#HL-CONFIG-001` | 等价 | 源码满足 | B14 正式 schema/配置 Owner；`GAP-HL-004/005`；Windows/用户确认未完成 |
| 7 | `HL-SEED-001` | `业务规则.md#HL-SEED-001` | `客户端规则.md#HL-SEED-001` | 等价 | 源码满足 | B14 版本化 `ModuleSeed`；`GAP-HL-005`；Windows/用户确认未完成 |
| 8 | `HL-LAUNCH-001` | `业务规则.md#HL-LAUNCH-001` | `客户端规则.md#HL-LAUNCH-001` | 等价 | 源码满足 | `GAP-HL-001`；Windows/用户确认未完成 |
| 9 | `HL-UPDATE-001` | `业务规则.md#HL-UPDATE-001` | `客户端规则.md#HL-UPDATE-001` | 等价 | 现场待验收 | `GAP-HL-007` |
| 10 | `HL-INSTALL-001` | `业务规则.md#HL-INSTALL-001` | `客户端规则.md#HL-INSTALL-001` | 等价 | 现场待验收 | `GAP-HL-008` |
| 11 | `HL-READY-001` | `业务规则.md#HL-READY-001` | `客户端规则.md#HL-READY-001` | 等价 | 源码满足 | `GAP-HL-010`；Windows/用户确认未完成 |
| 12 | `HL-NONBLOCK-001` | `业务规则.md#HL-NONBLOCK-001` | `客户端规则.md#HL-NONBLOCK-001` | 等价 | 源码满足 | `GAP-HL-009`；Windows/用户确认未完成 |
| 13 | `HL-DP-START-001` | `业务规则.md#HL-DP-START-001` | `客户端规则.md#HL-DP-START-001` | 等价 | 源码满足 | `GAP-HL-006`；Windows/用户确认未完成 |
| 14 | `CM-DP-001` | `业务规则.md#CM-DP-001` | `客户端规则.md#CM-DP-001` | 等价 | 源码满足 | B15 AP/CP `UploadTargets=None` 仍进主队列；`GAP-CM-001/006` 现场待验收 |
| 15 | `CM-DP-002` | `业务规则.md#CM-DP-002` | `客户端规则.md#CM-DP-002` | 等价 | 源码满足 | B15 独立 durable ingress 与 per-consumer 回执；`GAP-CM-002` 现场待验收 |
| 16 | `CM-DP-003` | `业务规则.md#CM-DP-003` | `客户端规则.md#CM-DP-003` | 等价 | 源码满足 | B13 三 worker 独立生命周期 + B15 本地 consumer 回执 |
| 17 | `CM-DP-004` | `业务规则.md#CM-DP-004` | `客户端规则.md#CM-DP-004` | 等价 | 源码满足 | B15 完整信封、队列满恢复、完成墓碑；`GAP-CM-002` 现场待验收 |
| 18 | `CM-ROUTE-001` | `业务规则.md#CM-ROUTE-001` | `客户端规则.md#CM-ROUTE-001` | 等价 | 源码满足 | B15 本地链不再由外部目标开关裁剪；`GAP-CM-001/006` |
| 19 | `CM-ROUTE-002` | `业务规则.md#CM-ROUTE-002` | `客户端规则.md#CM-ROUTE-002` | 等价 | 源码满足 | 目标随不可变入口信封冻结，Cloud/MES 独立补偿与死信 |
| 20 | `CM-GATE-001` | `业务规则.md#CM-GATE-001` | `客户端规则.md#CM-GATE-001` | 等价 | 现场待验收 | `ICloudExecutionPolicy`、MES heartbeat gate；真实双通道未验收 |
| 21 | `CM-ISO-001` | `业务规则.md#CM-ISO-001` | `客户端规则.md#CM-ISO-001` | 等价 | 源码满足 | Cloud/MES 独立数据库、worker、retry/fallback/deadletter owner |
| 22 | `CM-ISO-002` | `业务规则.md#CM-ISO-002` | `客户端规则.md#CM-ISO-002` | 等价 | 源码满足 | B13 三 worker 独立启动；B15 每消费者回执和 Cloud/MES 独立交接 |
| 23 | `CM-RETRY-001` | `业务规则.md#CM-RETRY-001` | `客户端规则.md#CM-RETRY-001` | 等价 | 源码满足 | 六类补偿存储、稳定身份与级联持久失败关闭 |
| 24 | `CM-RETRY-002` | `业务规则.md#CM-RETRY-002` | `客户端规则.md#CM-RETRY-002` | 等价 | 源码满足 | Cloud/MES 独立 retry task、gate 和冻结记录身份；现场恢复待验收 |
| 25 | `CM-RETRY-003` | `业务规则.md#CM-RETRY-003` | `客户端规则.md#CM-RETRY-003` | 等价 | 源码满足 | 第 20 次失败原子转本通道死信，零时间硬删除 |
| 26 | `CM-RETRY-004` | `业务规则.md#CM-RETRY-004` | `客户端规则.md#CM-RETRY-004` | 等价 | 源码满足 | retry 到 deadletter 同一 SQLite 事务，死信失败回滚并保留 retry |
| 27 | `CM-OPS-001` | `业务规则.md#CM-OPS-001` | `客户端规则.md#CM-OPS-001` | 受控替代 | 源码满足 | 本地管理员只能原子重入队；硬删除服务和 UI 入口均失败关闭 |
| 28 | `CM-DATA-001` | `业务规则.md#CM-DATA-001` | `客户端规则.md#CM-DATA-001` | 等价 | 源码满足 | `CellCompletedRecord`/`CellData` 同源事实 |
| 29 | `CM-DATA-002` | `业务规则.md#CM-DATA-002` | `客户端规则.md#CM-DATA-002` | 等价 | 源码满足 | Cloud/MES 独立目标适配器 |
| 30 | `CM-DATA-003` | `业务规则.md#CM-DATA-003` | `客户端规则.md#CM-DATA-003` | 等价 | 源码满足 | AP/CP `ClipNo` 及现有插件生产事实 |
| 31 | `CM-LOG-001` | `业务规则.md#CM-LOG-001` | `客户端规则.md#CM-LOG-001` | 等价 | 源码满足 | 直传/补传/阻塞/死信/恢复逐条记录 correlation、PlcCode、TaskKey 和业务标识 |
| 32 | `CM-LOG-002` | `业务规则.md#CM-LOG-002` | `客户端规则.md#CM-LOG-002` | 等价 | 源码满足 | 只有 provider 正式成功记为 `Uploaded`；本地 retry/fallback/deadletter 明确标注尚未上传 |
| 33 | `CM-LOG-003` | `业务规则.md#CM-LOG-003` | `客户端规则.md#CM-LOG-003` | 等价 | 源码满足 | `Blocked`/`Failed`/`RetryScheduled`/`DurableDeadLetter`/`Requeued` 稳定阶段语义 |
| 34 | `CM-LOG-004` | `业务规则.md#CM-LOG-004` | `客户端规则.md#CM-LOG-004` | 等价 | 源码满足 | Security 日志脱敏门禁与敏感配置规则 |
| 35 | `PLC-ID-001` | `业务规则.md#PLC-ID-001` | `客户端规则.md#PLC-ID-001` | 等价 | 现场待验收 | `GAP-PLC-009` |
| 36 | `PLC-RUNTIME-001` | `业务规则.md#PLC-RUNTIME-001` | `客户端规则.md#PLC-RUNTIME-001` | 等价 | 现场待验收 | `GAP-PLC-008/016` |
| 37 | `PLC-BIND-001` | `业务规则.md#PLC-BIND-001` | `客户端规则.md#PLC-BIND-001` | 等价 | 现场待验收 | `GAP-PLC-008` |
| 38 | `PLC-SEED-001` | `业务规则.md#PLC-SEED-001` | `客户端规则.md#PLC-SEED-001` | 澄清 | 部分满足 | 保留新播种 MG1/MG2 默认启用和 PLC 默认关闭；第三任务延期且不覆盖现场值；`GAP-PLC-001/012/013` |
| 39 | `PLC-TASK-001` | `业务规则.md#PLC-TASK-001` | `客户端规则.md#PLC-TASK-001` | 澄清 | 未满足 | MG1/MG2 已有，R100 第三任务延期；`GAP-PLC-002/022` |
| 40 | `PLC-READ-001` | `业务规则.md#PLC-READ-001` | `客户端规则.md#PLC-READ-001` | 受控替代 | 部分满足 | B16 已完成 MG1/MG2 按当前 `TaskKey` 原子按需读取本槽条码、数量、速度的完整同批快照，周期数量/速度只供通用采集、UI 与诊断；第三状态任务继续冻结；`GAP-PLC-003/006/011/020/022` |
| 41 | `PLC-BLOCK-001` | `业务规则.md#PLC-BLOCK-001` | `客户端规则.md#PLC-BLOCK-001` | 等价 | 源码满足 | B16 同前缀最小覆盖、100 words 上限、101 words 拆块与原子重组已实现；`GAP-PLC-004`；Windows/PLC 待验收 |
| 42 | `PLC-SCHED-001` | `业务规则.md#PLC-SCHED-001` | `客户端规则.md#PLC-SCHED-001` | 等价 | 源码满足 | B16 三级优先级、同级 FIFO、500ms—5s 等待老化与单 PLC 串行已实现；`GAP-PLC-007`；Windows/PLC 待验收 |
| 43 | `PLC-BUFFER-001` | `业务规则.md#PLC-BUFFER-001` | `客户端规则.md#PLC-BUFFER-001` | 受控替代 | 现场待验收 | 失败值改为类型默认值加失败质量，last-good 仅诊断；`GAP-PLC-006/020` |
| 44 | `PLC-QUALITY-001` | `业务规则.md#PLC-QUALITY-001` | `客户端规则.md#PLC-QUALITY-001` | 受控替代 | 现场待验收 | 禁止 block 后逐信号 fallback；`GAP-PLC-005/020` |
| 45 | `PLC-ONLINE-001` | `业务规则.md#PLC-ONLINE-001` | `客户端规则.md#PLC-ONLINE-001` | 受控替代 | 现场待验收 | 仅明确 Socket transport 才断联，取消旧累计阈值；`GAP-PLC-005/019` |
| 46 | `PLC-CLIP-001` | `业务规则.md#PLC-CLIP-001` | `客户端规则.md#PLC-CLIP-001` | 等价 | 现场待验收 | `GAP-PLC-006/020` |
| 47 | `PLC-CLIP-002` | `业务规则.md#PLC-CLIP-002` | `客户端规则.md#PLC-CLIP-002` | 澄清 | 现场待验收 | 固定三个不同成功新鲜 Generation；`GAP-PLC-020` |
| 48 | `PLC-ISOLATION-001` | `业务规则.md#PLC-ISOLATION-001` | `客户端规则.md#PLC-ISOLATION-001` | 等价 | 现场待验收 | `GAP-PLC-008/019` |
| 49 | `PLC-NAME-001` | `业务规则.md#PLC-NAME-001` | `客户端规则.md#PLC-NAME-001` | 澄清 | 源码满足 | B16 新 runtime 使用职责明确类名，旧名称仅作 Host API 2.0.x 薄包装；持久化 TaskKey 未改变；`GAP-PLC-010` |
| 50 | `PLC-FACTORY-001` | `业务规则.md#PLC-FACTORY-001` | `客户端规则.md#PLC-FACTORY-001` | 等价 | 部分满足 | MG1/MG2 候选已有，设备状态第三任务尚缺；`GAP-PLC-008/013` |
| 51 | `PLC-BIND-002` | `业务规则.md#PLC-BIND-002` | `客户端规则.md#PLC-BIND-002` | 等价 | 部分满足 | SQLite 唯一来源已实现，第三任务缺失；`GAP-PLC-013` |
| 52 | `PLC-BIND-003` | `业务规则.md#PLC-BIND-003` | `客户端规则.md#PLC-BIND-003` | 等价 | 现场待验收 | `GAP-PLC-016` |
| 53 | `PLC-BIND-004` | `业务规则.md#PLC-BIND-004` | `客户端规则.md#PLC-BIND-004` | 澄清 | 现场待验收 | 增加同 PLC 配置变更门与双向补偿；`GAP-PLC-014/015` |
| 54 | `PLC-BIND-005` | `业务规则.md#PLC-BIND-005` | `客户端规则.md#PLC-BIND-005` | 等价 | 现场待验收 | `GAP-PLC-015` |
| 55 | `PLC-BIND-006` | `业务规则.md#PLC-BIND-006` | `客户端规则.md#PLC-BIND-006` | 受控替代 | 现场待验收 | 改为先停止产生新状态、再原子保存并用新鲜确认租约恢复；`GAP-PLC-021` |
| 56 | `PLC-TASKSTATE-001` | `业务规则.md#PLC-TASKSTATE-001` | `客户端规则.md#PLC-TASKSTATE-001` | 澄清 | 现场待验收 | “最后成功”固定为最近成功启动/恢复；`GAP-PLC-017` |
| 57 | `PLC-CONN-001` | `业务规则.md#PLC-CONN-001` | `客户端规则.md#PLC-CONN-001` | 等价 | 现场待验收 | `GAP-PLC-018` |
| 58 | `PLC-CONN-002` | `业务规则.md#PLC-CONN-002` | `客户端规则.md#PLC-CONN-002` | 等价 | 现场待验收 | `GAP-PLC-016/019` |
| 59 | `PLC-ERROR-001` | `业务规则.md#PLC-ERROR-001` | `客户端规则.md#PLC-ERROR-001` | 澄清 | 现场待验收 | 固定六类稳定原因码与 Socket 优先；`GAP-PLC-019` |
| 60 | `PLC-READ-FAIL-001` | `业务规则.md#PLC-READ-FAIL-001` | `客户端规则.md#PLC-READ-FAIL-001` | 等价 | 现场待验收 | `GAP-PLC-020` |
| 61 | `PLC-WRITE-FAIL-001` | `业务规则.md#PLC-WRITE-FAIL-001` | `客户端规则.md#PLC-WRITE-FAIL-001` | 等价 | 现场待验收 | `GAP-PLC-019` |
| 62 | `PLC-STATUS-001` | `业务规则.md#PLC-STATUS-001` | `客户端规则.md#PLC-STATUS-001` | 澄清 | 未满足 | R100/Cloud-only 专项延期，禁止半链；`GAP-PLC-022` |

## 2. 宿主、插件、Launcher 与首启配置问题

| 问题编号 | 违反规则 | 当前源码表现 | 状态 | 源码证据 | 后续要求 | 验收要点 |
|---|---|---|---|---|---|---|
| GAP-HL-001 | `HL-SOURCE-001`、`HL-LAUNCH-001` | B13 将 `iiot-enabled-plugins.json` 收口为 activation 唯一选择源；只接受 `schemaVersion=1`，并将每个 `ModuleId` 精确绑定到唯一安全的 `pluginDirectory`。目录身份在 Windows 按不区分大小写比较，非 Windows 按精确大小写比较，大小写变体不能在敏感文件系统上冒充。缺失、损坏、版本不支持、身份重复或空清单失败关闭；选中目录、`plugin.json` 或 activation 缺失时形成按模块隔离的稳定诊断。`LauncherPluginActivationSource`、reconciler、profile 可见性和 `ShellLaunchService` 共用该选择；activation 贡献的卡片保留 `ModuleId + pluginDirectory` 来源，启动租约内重读当前 activation 并精确匹配原 profile/machine profile。未选目录、同模块替换目录或已不再由当前 activation 贡献的缓存卡片均不得启动。 | 源码候选满足，Windows/用户确认未完成 | `LauncherEnabledPluginSelectionSource.cs`；`LauncherPluginActivationSource.cs`；`LauncherPluginActivationReconciler.cs`；`LauncherProfileVisibilityService.cs`；`ShellLaunchService.cs`；`LauncherPluginActivationTests.cs`、`ShellLaunchServiceTests.cs` | 保持五个阶段共用同一选择源、schema、平台路径身份和租约内 activation/profile 身份且失败关闭；取得 Windows 和用户确认前保留本项。 | 清单缺失/损坏/未来版本/重复/空、选中目录缺失、同 ModuleId 冒充/替换目录、当前 activation 已替换 profile、大小写变体、手工投放未选插件和纯 Default 均不物化、显示或启动新工序；已选 AP/CP 的 Windows 候选验收另行完成。 |
| GAP-HL-002 | `HL-CLOUD-001`、`HL-META-001` | Cloud 首装包同时生成总 `iiot-binding.json` 和每插件 `iiot-plugin-binding.json`。Launcher 只消费总绑定并把 `BaseUrl/ClientCode/BootstrapSecret` 写入外部 machine profile；当前 EdgeClient 与 AP/CP 没有正式消费者读取每插件绑定文件，形成未消费的重复配置载体。 | 待后续处理 | `IIoT.CloudPlatform/src/services/IIoT.ProductionService/Commands/Human/ClientReleases/GenerateEdgeInstallerPackage.cs` 的 `BuildPluginBindingManifest`、`WritePayloadZip`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/Services/LauncherDeviceBindingImporter.cs` 的 `ApplyPendingBindings`、`WriteCloudApiIdentity` | 收敛为一条 Cloud 绑定传递链。以 Launcher 能事务落盘的总绑定为唯一输入；删除未定义消费者的重复绑定文件，或在另行确认契约前禁止其成为运行配置源。 | 首装后每个 profile 只有一份正式 Cloud 配置；断网重启仍有效；pending 成功消费后消失；任何 secret 不进入摘要和日志。 |
| GAP-HL-003 | `HL-CLOUD-001`、`HL-SOURCE-001` | B03-R 已把 release bootstrap 和 catalog 身份收敛到 Launcher 外部 machine profile，把 Host source、channel、runtime 收敛到外部 `launcher.update.json`；旧大小写键只做一次原子迁移，迁移后仅接受当前 camelCase。Host `appsettings`、打包 profile 和环境变量不再参与生产解析，正式 source 只接受 HTTP/HTTPS，catalog source 不一致时整份 catalog 无效且不改写外部配置。 | 源码具备关闭证据，Windows/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Configuration/FileEdgeUpdateConfigurationProvider.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Configuration/LauncherUpdateConfigurationFile.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Configuration/FileEdgeReleaseSourceValidator.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Configuration/FileEdgeUpdateConfigInitializer.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Updates/EdgeReleaseService.cs` | 保持外部已落盘配置为生产唯一 owner；禁止恢复环境变量、包内默认值、本地目录、非 HTTP source 或 catalog 反向改写。取得 Windows/用户确认前保留本行。 | 冲突环境变量、包内地址、file/FTP source 和 catalog Host source 的契约测试通过；运行值只来自外部配置，source 不一致按 catalog 无效处理且外部配置原字节不变，错误信息不泄密；Windows 候选另行确认。 |
| GAP-HL-004 | `HL-META-001`、`HL-CONFIG-001`、`HL-SEED-001` | B14 将 AP/CP `2.0.18` 的配置 Owner 收敛到插件 contributor 与正式 `Config/<module>.module.schema.json`：manifest 固定 schema 路径、`ModuleSeed` v1/Production、安全播种策略和静态能力，machine profile 只保留模块激活及版本/环境选择。Host 在完整 machine profile 生效后检查已启用插件契约，旧 `*.module.json` 永不进入运行配置；schema 缺失、身份不一致、越界、重复 Owner 或选择不匹配均形成稳定模块诊断，禁止按旧默认文件回退。 | 源码候选满足，Windows/用户确认未完成 | AP/CP `plugin.json`、`Config/*.module.schema.json`、`config/MachineProfiles/*.json`，Plugins B14 merge `f29e7713a3c11115a0a956597a3df3b0bc24ad9d`；Edge `DirectoryModuleCatalog.cs`、`ShellConfigurationLoader.cs`、`LauncherPluginActivationTests.cs`、`PluginCatalogLifecycleContractTests.cs`、`ShellConfigurationLoaderBehaviorTests.cs` | 保持一个正式配置 Owner、machine profile 只选择版本/环境、旧默认文件零运行影响和 schema/路径/重复身份失败关闭；取得 Windows/用户确认前保留本行。 | 全新 AP、全新 CP、同机 AP+CP 和二次启动时初值来源唯一；现场值重启/升级零覆盖；旧默认文件即使存在也只产生诊断；缺失、越界、错身份、重复 Owner 和错误版本/环境均不回退。 |
| GAP-HL-005 | `HL-CONFIG-001`、`HL-SEED-001` | B14 将 AP/CP contributor 和 Host 内部初始化链改为正式版本化 `ModuleSeed` 语义；Host API 2.0.x 仅为二进制兼容保留旧 SDK 注册槽。AP/CP 新 PLC 默认关闭、MG1/MG2 缺失绑定默认启用；Host writer 只按稳定 `PlcCode` 复用、逐项补缺失 IO/绑定并保留现场值，同名异码失败关闭，任何 `ResetBeforeImport` 在开启事务前拒绝。单模块播种失败只隔离该模块并允许后续启动重试；MES 已启用但地址、令牌、工站等必填角色缺失时只报告 `MES_CONFIGURATION_INCOMPLETE` 并阻断 MES，Shell/PLC 基础运行继续。R100/第三设备状态任务仍按 Cloud 专项前置门延期，不属于本次源码缺口。 | 源码候选满足，Windows/用户确认未完成 | AP/CP `ModuleSeed/*ModuleSeedContributor.cs`，Plugins B14 merge `f29e7713a3c11115a0a956597a3df3b0bc24ad9d`；Edge `ModuleSeedInitializer.cs`、`ModuleDevelopmentSeedWriter.cs`、`StartupMesConfigurationValidator.cs` 及对应 Application/Startup 测试 | 保持版本化选择、稳定身份、只补缺失、零 destructive reset、按模块失败隔离和 MES 局部诊断；第三任务继续由 `GAP-PLC-001/002/022` 管理，前置门满足前禁止半链。 | 全新 AP、全新 CP、同机 AP+CP、二次启动、局部升级、现场值修改、播种失败续跑和 MES 缺配置均由受影响测试覆盖；Windows/真实 PLC 与用户确认另行完成。 |
| GAP-HL-006 | `HL-DP-START-001`、`CM-ISO-002` | B13 删除三个任务的 `Host.DataPipeline` 组注册，将 `ProcessQueueTask`、`CloudRetryTask`、`MesRetryTask` 分别注册为独立长运行服务。每项分别保存启动/运行/停止/故障状态和稳定错误码；一项启动失败只清理该项，coordinator 继续启动其它两项，Shell 以对应局部诊断进入 `ReadyWithDiagnostics`。启动失败、Ready 后异常或无取消的干净退出会由 Host 以有界间隔只恢复该 worker，不重启兄弟任务；启动清理超时保持 `Faulted` 并跟踪唯一迟到停止，只有迟到停止安全成功后才恢复，迟到停止最终失败则继续 quarantine 且零重复启动。B15 又以 durable ingress 每 consumer 回执和 Cloud/MES 独立持久交接闭环数据内隔离；实时状态、变更 UTC、错误码和修复目标仍进入诊断页现有刷新链。 | 源码候选满足，Windows/用户确认未完成 | `DependencyInjection.cs`；`BackgroundServiceRuntimeStatus.cs`；`LongRunningBackgroundTaskService.cs`；`BackgroundServiceCoordinator.cs`；`AppLifecycleManager.cs`；`DataPipelineIngressStore.cs`；`ProcessQueueTask.cs`；`DiagnosticsViewModel.cs`；`DiagnosticsRowsBuilder.cs`；对应 Runtime Workflow/Persistence/UI 受影响测试 | 保持三项独立注册、状态、失败码、有界定向恢复、停止超时安全门和每 consumer/通道独立回执；取得 Windows 和用户确认前保留本项。 | 分别注入三个任务启动失败、Ready 后异常/干净退出或停止清理超时；只有失败项进入 `Faulted`，其它 worker 与已完成 consumer 不重启/重跑；诊断页不泄露原始异常；Windows 候选另行验证 `ReadyWithDiagnostics`。 |
| GAP-HL-007 | `HL-UPDATE-001` | B03-R 已把 Launcher 更新门控设为必需依赖并删除 Noop 降级。Shell 启动使用短租约，并在租约内重新读取已启用插件选择后才允许创建进程；唯一 catalog 更新入口取得租约后重新检查全部 Shell，并持有到下载、提交、回滚或 Host 重启交接完成。所有 Launcher 实例共享固定外部数据目录中的同一排他锁，异常、取消和进程退出均释放租约。 | 源码具备关闭证据，Windows/用户确认未完成 | `IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/Services/LauncherUpdateOperationGate.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/Services/ShellLaunchService.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/ViewModels/LauncherClientReleasePanelViewModel.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/App.axaml.cs` | 保持 Shell 启动、启动恢复和唯一更新入口共享同一跨进程门控；选择校验必须发生在启动租约内，门控缺失时禁止启动或更新。取得 Windows/用户确认前保留本行。 | Shell 已运行、更新期间启动 Shell、选择在预检与租约间被替换、两个 Launcher 并发、取消和异常释放锁的 Launcher Unit/Filesystem 测试通过；过期选择不得创建进程，第二个 Launcher 不进入下载或安装；Windows 候选另行确认。 |
| GAP-HL-008 | `HL-INSTALL-001` | B03-R 已删除 `EdgeReleaseService` 的逐包安装 fallback，包安装器只作为组合事务内部 prepare/commit 执行器。所有目标包的 HTTP/HTTPS 下载与完整校验都在正式写入前完成并在 staging 生成 `install.json`；插件目录、外部 profile、`Modules.Enabled`、依赖组合和 Host 交接由版本化 journal 统一提交，失败恢复和阻断保持 fail-closed。 | 源码具备关闭证据，Windows/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Packages/EdgePluginPackageInstaller.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Packages/EdgePluginCompositionTransaction.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Updates/EdgeReleaseService.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/App.axaml.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/Services/ShellLaunchService.cs` | 保持组合事务的先校验后提交、可恢复 journal、必需恢复服务和失败阻断语义；取得 Windows/用户确认前保留本行。Windows 安装候选与真实 Host 重启仍按候选验收流程另行执行。 | 安装记录、HTTP/HTTPS 包准备、目录移动、profile 写入、依赖准备、Host 交接、进程中断、取消、清理和回滚失败的 Update Contract 测试通过；本地/非 HTTP source 被拒绝；自定义配置、SQLite、账号与未受影响业务内容保持不变，journal 不含 secret；Windows 候选另行确认。 |
| GAP-HL-009 | `HL-NONBLOCK-001` | B13 将 Launcher fatal 边界缩小为 DI/框架/主窗口不可构造，主窗口显示后才由 `LauncherStartupCoordinator` 独立执行语言、本地账号、更新配置、恢复、activation 和绑定导入。可修复的 filesystem/path 失败精确限定为批准异常集合，以稳定原因码、修复入口和异常类型进入 Launcher 主 UI；未知实现异常和直接取消原实例传播，不得伪装为可修复配置错误。插件根枚举和逐插件错误分开诊断，不显示或记录原始异常消息。 | 源码候选满足，Windows/用户确认未完成 | `App.axaml.cs`；`LauncherStartupCoordinator.cs`；`LauncherStartupDiagnostics.cs`；`LauncherPluginActivationSource.cs`；`LauncherDeviceBindingImporter.cs`；`LauncherMainViewModel.cs`；Launcher Filesystem/Unit/UI 测试 | 保持 fatal 只包含不可构造边界，批准局部诊断继续脱敏且可修复，未知异常传播；取得 Windows 与用户确认前保留本项。 | 文件系统和 JSON fault injection 下仍显示主 UI 和对应诊断；注入未知实现异常时传播原实例；macOS 实际 Launcher 可视启动只作开发验证，Windows 候选仍需单独验收。 |
| GAP-HL-010 | `HL-READY-001` | B13 将启动握手升为 schema v2，显式携带 Shell PID，支持 `Ready`、`ReadyWithDiagnostics`、`Failed` 和结构化脱敏诊断。`ShellLaunchService` 同时校验本次启动 PID、machine profile、活动模块和状态；错误进程写入的 Ready、无效载荷、超时或提前退出均不得显示成功。Shell 在主窗口显示后将局部启动问题和写入 Ready 前的当前后台 worker 故障合并为 `ReadyWithDiagnostics`，不用较早启动汇总掩盖已故障任务。 | 源码候选满足，Windows/用户确认未完成 | `EdgeClientUpdateCoordination.cs`；`ShellLaunchService.cs`；Shell `App.axaml.cs`；`LauncherMainViewModel.cs`；`EdgeClientUpdateCoordinationTests.cs`、`ShellLaunchServiceTests.cs`、`CoreStabilityBehaviorTests.cs` | 保持 schema v2 身份、当前 worker 快照与脱敏载荷 fail-closed；取得 Windows 和用户确认前保留本项。 | 覆盖正常 Ready、外部离线降级 ReadyWithDiagnostics、写入前 worker 故障、目标插件拒载、错误 profile/PID、进程提前退出和超时；Windows 候选需再验证真实进程握手和外部离线降级。 |
| GAP-HL-011 | `HL-RELEASE-001` | B03-R 已将发布控制面从 `cloud-switch.projection.json` 解耦并按能力校验配置：业务 Cloud 关闭时运行心跳仍为零请求，缺少心跳路径只跳过心跳，缺少版本上报路径只使上报失败；合法首装绑定下 release bootstrap、catalog、下载和更新按钮继续工作。登录载入 profile 后自动检查一次，手动刷新复用同一 catalog 链；catalog 缺配置、401、超时、5xx、空数据、结构或版本非法、source 不一致时保留本地版本事实并显示“无法检查”，不存在旧 Host fallback。 | 源码具备关闭证据，Windows/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Update/Configuration/FileEdgeUpdateConfigurationProvider.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Updates/EdgeReleaseService.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Updates/EdgeRuntimeHeartbeatService.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/ViewModels/LauncherMainViewModel.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/ViewModels/LauncherClientReleasePanelViewModel.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Launcher/ViewModels/LauncherVersionManagementItems.cs` | 保持业务数据面与发布控制面分离、唯一 catalog 入口和按能力配置校验；禁止恢复旧 Host 行、本地 source 或把不可判定结果回落为“已最新”。取得 Windows/用户确认前保留本行。 | Cloud 业务关闭的 Runtime Workflow 零心跳测试通过；缺 heartbeat/report 路径不阻断 catalog；隔离目录和 loopback catalog 的真实 Launcher 容器/UI 验证 AP/CP `2.0.10 → 2.0.11` 自动检查、手动刷新及 5xx 失败态，未连接生产 Cloud；Windows 候选另行确认。 |

## 3. Cloud/MES 双链路问题

| 问题编号 | 违反规则 | 当前源码表现 | 状态 | 源码证据 | 后续要求 | 验收要点 |
|---|---|---|---|---|---|---|
| GAP-CM-001 | `CM-DP-001`、`CM-ROUTE-001` | B15 已删除 AP/CP `UploadTargets=None` 时跳过 DataPipeline 的路径；每个真实完工均先交给 Host 主队列，开关只冻结外部目标。只有主内存通知或完整持久入口可靠接受后，插件状态才提交。 | 源码候选满足，Windows/现场/用户确认未完成 | AP/CP `*ClipStateProcessor.cs`、`*ClipStateProcessorBehaviorTests.cs`；Plugins B15 merge `ba13b5d3498b58de6ad2d438d5fcb2a766d96d2d` | 保持本地完工链与外部目标开关解耦；取得 Windows/现场和用户确认前保留本项。 | MES/Cloud 均关闭时仍执行 Capacity、Excel、生产 SQLite 和 UI，且不产生外部失败记录；完整入口落盘失败时不提交生产状态。 |
| GAP-CM-002 | `CM-DP-002`、`CM-DP-004` | B15 以独立 `pipeline_ingress.db` 保存首次不可变完工信封、每 consumer 回执和完成墓碑。队列满只丢内存唤醒，500 ms 恢复器按接受顺序继续；已完成 consumer 不重跑，必需本地效果和外部持久交接全部完成后才清理信封。旧 `IngressOverflowPersistence` 生产实现与 DI 注册已删除，生产 `DataPipelineService` 必须解析 durable ingress。 | 源码候选满足，Windows/现场/用户确认未完成 | `IDataPipelineIngressStore.cs`、`DataPipelineIngressStore.cs`、`DataPipelineService.cs`、`ProcessQueueTask.cs`；`DataPipelineIngressStoreBehaviorTests.cs`、`DataPipelineServiceBehaviorTests.cs`、`ProcessQueueTaskBehaviorTests.cs` | 保持完整入口为生产唯一溢出恢复路径；现场验收前不宣称已投产。 | 队列第 5001 条仍持久接受；consumer 局部失败只重试未完成项；无内存通知的重启可全链重放；完整落盘失败返回拒绝。 |
| GAP-CM-003 | `CM-RETRY-003`、`CM-RETRY-004` | B15 将 Cloud/MES 第 20 次失败改为本通道 SQLite 内原子 `retry → deadletter`：先写入并校验死信，再删除 claim/retry。死信写入或删除任一失败会整体回滚，原 retry 保留；不再以 `DateTime.MaxValue` 伪装弃置。 | 源码候选满足，Windows/真实 Cloud/MES/用户确认未完成 | `RetryRecordProcessorBase.cs`、`RetryRecordStoreBase.cs`、`CloudRetryRecordStore.cs`、`MesRetryRecordStore.cs`；`RetryTaskBehaviorTests.cs`、`RetryDeadLetterAtomicTransitionBehaviorTests.cs` | 保持 Cloud/MES 分库、分表、分 claim 和同通道原子转移；现场完整恢复链待验收。 | 分别覆盖第 20 次失败、死信写入成功、死信写入失败回滚和唯一可消费源。 |
| GAP-CM-004 | `CM-RETRY-003`、`CM-OPS-001` | B15 删除时间保留期硬删除调度，Host API 2.0.x 保留的 `DeleteExpiredAbandonedAsync` 固定返回 0。死信底层硬删除失败关闭，Avalonia 诊断页删除入口已移除；本地管理员只能执行“原通道 retry 写入 + deadletter 可消费源移除 + audit”同一事务，任一失败保留唯一原记录。 | 源码候选满足，Windows/用户确认未完成 | `RetryHousekeepingServiceBase.cs`、`RetryRecordStoreBase.cs`、`DeadLetterStoreBase.cs`、`DeadLetterMaintenanceService.cs`、`DiagnosticsPage.axaml`；`FailedRecordStoreBehaviorTests.cs`、`RetryDeadLetterAtomicTransitionBehaviorTests.cs`、`DeadLetterMaintenanceBehaviorTests.cs`、`DiagnosticsViewModelBehaviorTests.cs` | 保持未成功 retry/fallback/deadletter 零时间硬删除和本地管理员受控原子重入队；取得用户确认前保留本项。 | 时间推进超过 30 天仍零删除；无 UI/服务硬删除；重入队成功写 audit，失败整体回滚。 |
| GAP-CM-005 | `CM-LOG-001`、`CM-LOG-002` | B15 将 MES/Cloud 直传、补传、阻塞、retry/fallback/deadletter 落盘和人工恢复统一为逐条 `CorrelationId + PlcCode + ModuleId/ProcessType + TaskKey + BusinessId` 追溯。AP/CP `BusinessId` 由 `CellData.DisplayLabel` 取稳定弹夹号；只有 provider 正式成功记为 `Uploaded`，本地落盘明确标注“尚未上传成功”，且日志只显示稳定原因码与异常类型。 | 源码候选满足，Windows/真实 Cloud/MES/用户确认未完成 | `UploadTraceLogFormatter.cs`、`MesConsumer.cs`、`CloudConsumer.cs`、`DataPipelineLogContext.cs`、`CloudRetryRecordProcessor.cs`、`MesRetryRecordProcessor.cs`、`DataPipelineCascadingPersistenceWriter.cs`；MES Contract/Cloud Consumer/Retry/DeadLetter 受影响测试 | 保持每条业务事实可检索、阶段语义真实和日志脱敏；真实 Cloud/MES 对账待后续独立窗口。 | AP/CP 直传与补传的成功、失败、死信可按弹夹号逐条检索；本地落盘不冒充上传成功；token/secret 原文不进日志。 |
| GAP-CM-006 | `CM-DP-001`、`CM-ROUTE-001` | 本项与 `GAP-CM-001` 为同一缺口；B15 已在 `GAP-CM-001` 下形成源码候选和测试证据，本项不再存在独立实现工作。 | 已并入 GAP-CM-001，待用户确认 | `GAP-CM-001`；AP/CP `*ClipStateProcessor.cs`；Plugins B15 merge `ba13b5d3498b58de6ad2d438d5fcb2a766d96d2d` | 后续现场验收和关闭证据只归 `GAP-CM-001`；取得用户确认前保留本行，禁止静默删除。 | `GAP-CM-001` 继续承载本地-only 主队列的 Windows/现场验收；用户确认合并裁决后才能关闭本项。 |

## 4. 宿主与 Cloud/MES 后续处理顺序

1. 后续批次开始前固定授权仓库基线；Cloud、SDK、Private Plugins 未获写入授权时只读。
2. 逐条处理 `GAP-HL-*`、`GAP-CM-*`；完成项必须取得源码和受影响测试证据，禁止仅凭工作区修改状态判定已解决。
3. 先收敛配置 owner、首装来源、Ready 契约和安装事务，再处理 Launcher/UI 状态，禁止 UI 先伪装成功。
4. Cloud/MES 整改必须保持双通道物理隔离和本地完工零丢失，不得以简化为由合并队列、删除补偿或删除死信。
5. 完成实现与验收后先记录关闭证据；取得用户明确确认前不得删除对应 GAP。62 条稳定规则虽已迁移 62/62，原 `业务规则.md` 与正式规则仍允许暂时重复；用户确认删除前不得宣称原文已退出活动树。

## 5. B01/B02 边界

- B01 只收口正式规则、遗留 GAP 和 Git 历史，不修改生产代码、测试、数据库或配置。
- B02 只恢复规则原文、纠正文档裁决并修复既有 CI 测试门禁，不修改生产业务源码、数据库或配置。
- B01/B02 不新增或改变公共 API、接口、数据表或运行时行为。
- B01 未运行测试、构建、打包或部署；B02 只运行获授权的静态门禁和受影响测试，不运行全量、Coverage、Quality、打包或部署。

## 6. PLC 启动、读取与任务问题

本章节保留 B01 基线仍未满足或部分满足的既有 PLC 专项差距，不复用已删除编号。

| 问题编号 | 违反规则 | 当前源码表现 | 状态 | 源码证据 | 后续要求 | 验收要点 |
|---|---|---|---|---|---|---|
| GAP-PLC-001 | `PLC-SEED-001`、`HL-SEED-001` | B14 已用 AP/CP `2.0.18` 正式版本化 `ModuleSeed` 播种 PLC、IO 与 MG1/MG2 绑定：新 PLC 默认关闭、缺失 MG1/MG2 绑定默认启用，Host 只按稳定 `PlcCode` 复用并逐项补缺失行，保留现场参数和任务选择，禁止名称认领、覆盖及 destructive reset。剩余缺口仅是受前置门冻结的设备状态第三任务及其 R100 默认映射/绑定。 | 部分满足；MG1/MG2 源码满足，第三任务延期 | AP/CP `ModuleSeed/*ModuleSeedContributor.cs`、`Config/*.module.schema.json`，Plugins B14 merge `f29e7713a3c11115a0a956597a3df3b0bc24ad9d`；Edge `ModuleDevelopmentSeedWriter.cs`、`ModuleSeedInitializer.cs` 及对应测试 | 保持现有幂等补齐、MG1/MG2 默认启用和 PLC 默认关闭语义。只有取得 PLC 方对 R100 地址、类型和状态语义的正式确认并另行授权 Cloud 专项后，才允许补入设备状态候选、R100 模板和默认绑定；升级仍只补缺失，当前禁止先播种半条链。 | 当前验收 MG1/MG2 缺失绑定补齐、PLC 默认关闭和二次执行零覆盖；前置门满足并完整引入设备状态链后，再验收全新与升级 AP/CP profile 的第三绑定。 |
| GAP-PLC-002 | `PLC-TASK-001` | AP/CP 当前任务工厂各自只声明 MG1、MG2 两个候选任务，没有独立设备状态任务；当前 AP/CP 信号模板也没有恢复历史 `R100` 设备状态点。 | 待后续处理 | `IIoT.Edge.Plugins.Private/src/IIoT.Edge.Module.AP/Production/ApStationRuntimeFactory.cs`、`IIoT.Edge.Plugins.Private/src/IIoT.Edge.Module.CP/Production/CpStationRuntimeFactory.cs`；`IIoT.Edge.Plugins.Private/src/IIoT.Edge.Module.AP/Config/Io/APPlcSignals.cs`、`IIoT.Edge.Plugins.Private/src/IIoT.Edge.Module.CP/Config/Io/CPPlcSignals.cs` | 只有取得 PLC 方对 R100 地址、类型和状态语义的正式确认并另行授权 Cloud 专项后，才允许为 AP、CP 增加可独立绑定的设备状态任务及 `R100 / Int16 / 1 word` 默认映射；当前不得实现半条链，后续仍须保留地址现场可改能力。 | 前置门满足并完整引入设备状态链后，每台适用 PLC 显示 MG1、MG2、设备状态三个独立候选，分别启停互不影响，状态码 `-1/0/1/2/3` 可读取和诊断。 |
| GAP-PLC-003 | `PLC-READ-001`、`PLC-TASK-001` | B16 将现有 MG1/MG2 收敛到 Host 唯一混合读取链：binder 只把候选任务独占的 ASCII 条码标记为业务按需读取并从周期扫描排除，数量和速度继续周期发布供通用采集、UI 与诊断；`PlcBusinessOnDemandReadCoordinator` 在同一 PLC service 和统一调度门内原子读取当前 TaskKey 的完整信号组。AP/CP `2.0.19` 每轮各只请求一次本槽条码、数量和速度，并只消费同一 `Generation/BatchId/采集 UTC` 的不可变快照，禁止 live buffer 或旧周期值补齐。第三设备状态任务和 R100 仍冻结。 | 部分满足；MG1/MG2 源码候选满足，设备状态延期；Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcRuntimeTaskBinder.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcBusinessOnDemandReadCoordinator.cs`、`PlcDeviceRuntimeBuilder.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Signals/PlcDataReadScanTask.cs`；AP/CP `*StationRuntimeFactory.cs`、`*ClipScanTask.cs` 及对称 Business 测试；SDK merge `fe8b4c46c48197eecbeaf74a8463cd347edf9129`、Edge merge `96be337928e2debb1f460baefe436a1e461b4f95`、Plugins merge `183adc65eb4299f216de136490285bdc482b03ab`；B17 exact-SHA 尚未运行 | 保持 MG1/MG2 请求集合隔离、周期条码零扫描、同批不可变结果和失败质量关闭；B17 只在另行授权后冻结 exact-SHA 并执行 Windows/PLC 验收。设备状态必须继续等待 PLC 正式确认和 Cloud 专项授权，禁止补半条链。 | MG1 请求只包含 MG1 条码、数量和速度，MG2 对称隔离；条码、数量、速度、质量和时间来自同一批；周期批读保留数量/速度但不读弹夹码；混批、过期或失败不推进；真实 Windows/PLC 另行验收。 |
| GAP-PLC-004 | `PLC-BLOCK-001` | B16 的 `PlcSignalBlockPlanner` 已把同前缀读取按最小覆盖区间合并，默认及硬上限为 100 words；超过上限的单映射或跨映射覆盖区间会拆为连续 fragment，周期读取与按需读取均只在全部 fragment 成功后原子重组，任一后续 fragment 失败则整项质量失败。写入仍保留独立安全 gap 策略，并按原意图切片。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcSignalBlockPlanner.cs`、`PlcBusinessOnDemandReadCoordinator.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Signals/PlcDataReadScanTask.cs`、`PlcIoScanTask.cs`；`PlcIoScanTaskBehaviorTests.cs`；Edge merge `96be337928e2debb1f460baefe436a1e461b4f95` | 保持读取最小覆盖、100 words 上限、跨 fragment 原子质量与写入独立 gap 策略；B17 现场验收前保留本项。 | `D101/D103/D108` 只产生 `D101 × 8`；100 words 合并、101 words 拆分并原子重组；后段失败整项失败；不同前缀不合并；写入 101 words 保留完整意图。 |
| GAP-PLC-005 | `PLC-QUALITY-001`、`PLC-ONLINE-001` | B01 基线的 `PlcDataReadScanTask` 已不再把 block 失败降级为逐信号重读，而是发布类型默认值、失败质量、批次和失败时间；`PlcBuffer` 也保留 last-good 仅供诊断。原“失败保留最后成功值供业务读取”的整改描述已被最新质量规则取代，剩余原子消费问题归入 `GAP-PLC-006/020`。 | 已取代，待用户确认 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Signals/PlcDataReadScanTask.cs` 的 `StageFailedBlock`、`CommitReadBatch`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/Store/PlcBuffer.cs` 的 `PublishReadBatch`、`CreateNextReadState` | 保留“失败默认值 + 失败质量、last-good 仅诊断、无单信号 fallback”最新口径；在 `GAP-PLC-006/020` 完成原子消费前不得删除本记录，最终删除还须用户确认。 | block 失败只有一次协议请求；失败默认值不得进入生产、完工或状态机；last-good 只能出现在诊断；`GAP-PLC-006/020` 验收通过且用户明确同意后方可移除。 |
| GAP-PLC-006 | `PLC-BUFFER-001`、`PLC-CLIP-001` | SDK 已提供不可变批次及强类型快照契约；Host 的 `PlcBuffer`、周期读取和 B16 按需读取协调器都通过原子接口发布并深复制捕获结果；AP/CP 每个 MG cycle 只捕获一次，条码、数量、速度、质量和时间均同属一个 `Generation/BatchId`，捕获后不再读取实时 Buffer。新鲜度门会拒绝未来、过期、时间不递增、Generation 回退和混批快照。B04 历史候选已有兼容证据；B16 当前三仓源码候选尚未运行 B17 exact-SHA。 | 源码候选满足；B16 exact-SHA、真实 Windows/PLC 和用户确认未完成 | SDK B04-A merge `7b63b15956c732cf4e05e33d57c66f3bde99f43b`；Edge B04-B merge `33db59d461f89e6822203bcb88b47ab5308d0c60`；Plugins B04-R-A merge `16070216ffc7a0a76c17cb50e0e4064ea31f3411`；B04 兼容证据 `IIoT.Edge.Sdk/artifacts/compatibility/b04r-rerun2-host-33db59d4-sdk-0285df83-plugins-16070216.json`；B16 当前 merge 见本文件第 1 节 | 保持一次捕获、同批解码和配置新鲜窗口；B17 exact-SHA、真实 Windows 与 PLC 验收及用户确认完成前保留本行，不得宣称现场闭环或生产完成。 | 并发发布只能消费完整 N 或 N+1；码、数量、速度和时间同批；过期、混批、回退或失败质量均不推进业务；B17 兼容证据须另行生成且 `status=passed`、`publishedBaselines` 非空；真实 Windows/PLC 另行验收。 |
| GAP-PLC-007 | `PLC-SCHED-001`、`PLC-RUNTIME-001` | B16 已把同一 PLC 的连接/交互/必要写入、业务按需读取和周期读取统一纳入一个 `PlcOperationGate` 调度队列，分别使用 Interactive、BusinessOnDemand、Periodic 三级优先级；同级按入队序号 FIFO。每个请求按当前扫描间隔并限制在 500ms—5s 的老化窗口逐级提升直至最高级，持续高优先级压力下低优先级仍有界执行。单 PLC 始终只有一个 active lease，不同 PLC 的 gate 保持并行；Closing/quarantine 所有权语义不变。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/Services/PlcOperationGate.cs`、`PlcOperationSchedulingContext.cs`；`PlcOperationGateBehaviorTests.cs`、`PlcBusinessOnDemandReadCoordinatorBehaviorTests.cs`、`PlcIoScanTaskBehaviorTests.cs`；Edge merge `96be337928e2debb1f460baefe436a1e461b4f95` | 保持三级优先级、同级 FIFO、500ms—5s 老化、单 PLC 单 active lease、不同 PLC 并行和原关闭/quarantine 语义；B17 现场验收前保留本项。 | 交互请求优先于等待中的业务/周期请求；同级顺序稳定；持续交互压力下周期请求经老化有界执行；同 PLC 最大并发为 1，不同 PLC 可并行；关闭和隔离不泄漏 waiter 或 lease。 |
| GAP-PLC-008 | `PLC-BIND-001`、`PLC-ISOLATION-001` | B08 已在 B05-A 的逐 `TaskKey` 隔离基础上，把任务计划及启动/绑定配置诊断统一为稳定 `PlcCode + TaskKey + SignalKey`；候选缺失、重复或信号不兼容仍只隔离对应 TaskKey，PLC 基础连接、Shell 和其它有效任务继续。单任务运行故障同样只隔离对应 TaskKey，但其诊断按实际可定位范围携带 `PlcCode + TaskKey`，不得虚构 SignalKey。源码候选已满足任务配置诊断要求，尚无真实 Windows/PLC 运行证据。 | 部分满足，真实 Windows/PLC 和用户确认未完成 | `IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcRuntimeTaskBinder.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/StartupPlcConfigurationValidator.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcDeviceRuntimeHandle.cs`；Edge B08 final merge `2d85cfdda617eaf4cc67c97967109b5f62b1f8dc` | 保持 TaskKey 级失败隔离和任务配置三项稳定诊断；在真实 Windows/PLC 上确认一个无效任务不会影响同 PLC 其它有效任务。取得用户确认前保留本项，不直接关闭。 | MG1 配置无效时 MG2 和设备状态仍运行；MG2 无效不影响 MG1；任务配置诊断准确包含 `PlcCode`、TaskKey 和错误信号；运行期未知异常至少包含 `PlcCode + TaskKey` 且不虚构错误信号；真实 Windows/PLC 与用户确认另行完成。 |
| GAP-PLC-009 | `PLC-ID-001` | B08 已把 SDK 生产上下文、Edge runtime/持久补传/诊断和 AP/CP catalog/runtime 统一到不可变 `PlcCode`；`DeviceName` 只作当前展示/MES 字段，`NetworkDeviceId` 只作本地行定位。旧上下文和六类持久记录只允许从已有 PlcCode、CellData 稳定 `DeviceCode` 或唯一 `NetworkDeviceId` 映射迁移，冲突记录保留并失败关闭；新 Cloud 幂等键使用不依赖名称和本地行号的 V2，旧记录继续 V1。B12 又收紧旧产能缓冲：先精确匹配当前 PlcCode，未命中时只接受持久化别名表中唯一且未复用的已验证历史别名，任一当前 DeviceName 即使已记入别名表也不得认领；别名损坏、名称复用、配置读取失败或无法唯一解析时保留原记录、零上传、零删除，并释放已取得的 claim 等待人工修复。补传以原始 RecordId 游标跨轮继续，每轮仍严格限制三批；阻断记录不会饥饿后续有效记录，也不会无界占用 Cloud retry 链。Host `2.0.14` 仅作为本批源码候选版本，未打包、未运行三仓 exact-SHA、未发布。 | 源码候选满足，真实 Windows/PLC 和用户确认未完成 | `IIoT.Edge.Sdk/src/IIoT.Edge.Module.Contracts/Primitives/Context/ProductionContext.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.DataPipeline/Context/ProductionContextStore.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.DataPipeline/Context/PersistentPlcIdentityAliasRegistry.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Integration/Capacity/CapacitySyncTask.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Persistence.Dapper/Stores/CapacityBufferStore.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Persistence.Dapper/Stores/DataPipelineIdentityMigration.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcLifecycleCoordinator.cs`；`IIoT.EdgeClient/src/Tests/IIoT.Edge.Runtime.WorkflowTests/CapacitySyncTaskBehaviorTests.cs`；`IIoT.EdgeClient/src/Tests/IIoT.Edge.Persistence.Tests/CapacityBufferCursorStoreBehaviorTests.cs`；AP/CP `*DeviceCatalog.cs`、`*StationRuntimeFactory.cs`；SDK merge `e7a746d22b49215344c2e5cdaca3b3b9e5536cb2`、Edge 主实现 merge `c463c043aca19b1e97d08619c05052de3d2b2b05`、Edge 生命周期日志补正 merge `2d85cfdda617eaf4cc67c97967109b5f62b1f8dc`、Plugins merge `61813e303c84362c3f2c8a914b82c040f21ce551`；B12 最终 main SHA 与三仓终态 exact-SHA 留待后续候选冻结 | 保持插件、上下文、任务事实、持久补传和诊断按 PlcCode 归属；旧产能缓冲只允许稳定代码或唯一持久历史别名解析，禁止恢复当前名称直认领，并保持有界游标继续。真实 Windows/PLC 验收设备改名、同 PlcCode 重建、重复 PlcCode、历史别名、名称复用和旧缓冲补传；取得用户确认前保留本项。 | PlcCode 新行和唯一历史别名可上传稳定代码；当前名称即使已在别名表中、别名文件损坏、名称复用及权威配置读取失败均保持原记录且不上传、不删除；已领取阻断记录当轮释放 claim，游标下轮从后续 RecordId 继续；设备改名、同 PlcCode 重建和重复 PlcCode 拒绝继续成立；真实 Windows/PLC 与用户确认另行完成。 |
| GAP-PLC-010 | `PLC-NAME-001` | B16 的新 runtime 已分别实例化 `PlcSignalInteractionTask` 和 `PlcPeriodicBatchReadTask`；日志与诊断使用对应明确职责名称。原 `PlcIoScanTask`、`PlcDataReadScanTask` 只作为 Host API 2.0.x 二进制兼容薄包装委托新实现，不注册为第二套生产任务；MG1/MG2 的持久化 TaskKey 和插件弹夹任务名称未改变。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Signals/PlcIoScanTask.cs`、`PlcDataReadScanTask.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcDeviceRuntimeBuilder.cs`；`PlcIoScanTaskBehaviorTests.cs`；Edge merge `96be337928e2debb1f460baefe436a1e461b4f95` | 保持新 runtime 只使用职责明确类型、旧名零第二生产路径和持久化 TaskKey 不变；旧薄包装只能在 Host API 2.1.0 显式退出批次删除。 | DI/runtime 实例、日志和诊断能区分信号交互、周期批读及 MG1/MG2 弹夹；旧包装行为等价且不重复启动；升级后原 TaskKey 绑定继续生效。 |
| GAP-PLC-011 | `PLC-READ-001`、`PLC-ISOLATION-001` | B16-A 在 SDK `2.0.11` 增加唯一 `PlcIoTypeWordLengthValidator`，固定 `Bool/Int16/UInt16=1 word`、`Int32/UInt32/DWord/Float=2 words`、`Ascii=正长度` 及连续映射对齐规则。B16-B 已在 IO 保存、硬件草稿/UI、任务绑定校验和 runtime 构建共用该规则；保存或绑定无效时以稳定原因失败关闭，运行期只跳过无效映射并隔离依赖 TaskKey，PLC 基础 runtime 与其它有效任务继续。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.Edge.Sdk/src/IIoT.Edge.Module.Sdk/Hardware/PlcIoTypeWordLengthValidator.cs`；Edge `SaveIoMappings.cs`、`HardwareConfigDraftValidator.cs`、`PlcTaskBindingService.cs`、`PlcDeviceRuntimeBuilder.cs`；对应 Application、Runtime Workflow、Shell UI 测试；SDK merge `fe8b4c46c48197eecbeaf74a8463cd347edf9129`、Edge merge `96be337928e2debb1f460baefe436a1e461b4f95` | 保持保存、绑定、runtime 和 UI 共用同一 SDK 校验器，禁止复制或放宽类型表；B17 现场验收前保留本项。 | Int16/UInt16/Bool、Int32/UInt32/DWord/Float、ASCII 的合法和非法长度均一致裁决；非法保存零写入，非法绑定只隔离相关 TaskKey，runtime 不拖停基础连接或其它任务。 |
| GAP-PLC-012 | `PLC-SEED-001`、`PLC-QUALITY-001`、`PLC-ID-001` | B02 把三个已有独立归口的缺口重复聚合到本项：质量与同批原子消费已由 `GAP-PLC-006/020` 承载，稳定 `PlcCode` 业务身份由 `GAP-PLC-009` 承载，62 条规则迁移由 `GAP-PLC-023` 承载；本项没有剩余独立验收条件。 | 已拆分归口，待用户确认 | `GAP-PLC-006`、`GAP-PLC-009`、`GAP-PLC-020`、`GAP-PLC-023` | 后续实现和关闭证据分别进入上述编号；取得用户确认前保留本行，禁止静默删除或再次合并裁决。 | 四个归口编号保持可定位且完整承载原条件；活动引用完成迁移；用户确认拆分裁决。 |

## 7. PLC 既有未完成项处理顺序

1. B12—B16 已形成当前 Host、SDK、Private Plugins 功能实现基线；B17 开始前必须重新读取三仓 clean `main=origin/main`，确认其 tree 包含本文件第 1 节所列实现 merge，并以届时实时 SHA 冻结 exact 候选；任一功能代码漂移都必须停止并重新验证。
2. `GAP-PLC-003/004/007/010/011` 的非 Cloud 源码部分已经完成；B17 必须另行授权后再运行 exact-SHA、Validate、Prepare 和 Windows/真实 PLC 验收，不能把本轮 Business 测试替代兼容或现场证据。
3. `GAP-PLC-001/002/003/013/022` 涉及设备状态第三任务或 R100 的部分继续冻结；必须等 PLC 方正式确认候选口径并另行授权 Cloud 专项后，作为完整 Cloud-only 链串行处理，禁止制造半条链。`GAP-PLC-005` 与 `GAP-PLC-012` 继续只保留原裁决和用户确认门。
4. 读取链必须保持“不同 PLC 并行、同一 PLC 单连接串行、局部任务失败隔离”；禁止用多连接、全 PLC 停机或 live-buffer fallback 替代。
5. B17 的 AP/CP 状态机验收必须覆盖条码失败、真实空码、连续三次空码、数量/速度过期、100/101 words、持续高优先级压力和 MG1/MG2 隔离。
6. 62 条稳定规则已完成 62/62 迁移；取得用户明确确认前，原 `业务规则.md` 不得删除，正式规则与受保护原文允许暂时重复。

## 8. PLC 动态任务绑定、连接与重连问题

本章节恢复 B01 曾移除的编号，并保留仍未满足、部分满足或尚待用户确认的动态任务、连接和重连问题；禁止再次静默删除。

| 问题编号 | 违反规则 | 当前源码表现 | 状态 | 源码证据 | 后续要求 | 验收要点 |
|---|---|---|---|---|---|---|
| GAP-PLC-013 | `PLC-BIND-002` | `PlcTaskBindingService.ResolveEnabled` 已只接受存在且启用的 SQLite 行，环境默认和候选 `DefaultEnabled` 不再决定运行态；`ModuleDevelopmentSeedWriter` 已幂等补齐插件声明的缺失绑定。AP/CP 当前只声明 MG1、MG2，缺少设备状态候选及其绑定，完整三任务首启/升级条件仍未满足。内部 `PLC-STATUS-001` 已记录 `R100 / Int16 / 1 word` 和 `-1/0/1/2/3` 候选语义，但尚未取得 PLC 方正式确认，也未授权 Cloud 专项；当前不得提前制造待上传事实。 | 部分满足 | `IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Hardware/PlcTaskBindings/PlcTaskBindingService.cs` 的 `ResolveEnabled`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Modules/Samples/ModuleDevelopmentSeedWriter.cs`；AP/CP `*DevelopmentSampleContributor.cs` 的 `TaskBindings` | 保留 SQLite 单一来源和缺行关闭语义；取得 PLC 方对现有 R100 地址、类型和状态语义的正式确认并另行授权 Cloud 专项后，设备状态候选才可由同一播种链补齐第三绑定。 | 环境配置和 `DefaultEnabled` 不能改变缺行结果；全新/升级 AP、CP 均有三个绑定，删除任一行只关闭该任务并显示“绑定缺失”。 |
| GAP-PLC-014 | `PLC-BIND-004` | B05-B 已删除页面的“先保存 SQLite、再独立应用 runtime”双入口；页面只调用 `SaveAndApplyAsync`。协调事务按单台 PLC 串行执行完整校验与原状态快照、SQLite 单事务提交和 `TaskKey` 增量应用；绑定事务与同设备的硬件、端口、协议帧、共享 IO 保存及插件标准点位重置共用配置变更门。整页硬件命令先发现受影响 PLC，再进入按设备排序的门集合；门内重新读取权威快照，并持有至 SQLite 提交、删除设备停机和变更设备重载全部结束。插件标准点位重置也在写入 IO 映射前进入同一门，并持有至 runtime 重载结束；事务快照、应用和补偿期间不能被另一映射或运行配置变更穿插。运行时失败会恢复原任务组合和原 SQLite 行，回滚失败返回显式事务故障。失败后页面重新读取 SQLite 真值，PLC 离线则保存成功并返回“等待 PLC”。B12 在事务业务入口及取得共享配置变更门后、持久化快照前分别复核 `CanEditHardware`；页面 `CanExecute` 不再是唯一权限边界，直接调用或等待期间权限被撤销均以零写入、零 runtime 动作失败关闭。 | 源码候选满足，Windows/PLC/用户确认未完成 | `IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcTaskBindingTransactionService.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Common/Plc/PlcRuntimeConfigurationMutationGate.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Hardware/HardwareConfig/HardwareConfigQueries.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Hardware/HardwareConfig/HardwareConfigCrudService.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Hardware/PlcTaskBindings/PlcTaskBindingService.cs`；`IIoT.EdgeClient/src/Presentation/IIoT.Edge.Presentation.Navigation/Features/Hardware/PlcTaskBindingView/ViewModels/PlcTaskBindingViewModel.cs`；`IIoT.EdgeClient/src/Tests/IIoT.Edge.Shell.UiTests/PlcTaskBindingTransactionBehaviorTests.cs`；对应 Persistence、Application、Shell UI、Startup Integration 与 Runtime Workflow 测试 | 保持保存和应用只有一个业务命令、业务入口权限 fail-closed、关键依赖 fail-closed、所有 IO 映射写入口从持久化前至停机/重载后的同设备配置变更互斥、双向补偿及失败后重读数据库真值；取得真实 Windows、PLC 和用户确认前保留本行。 | 无硬件权限的直接调用及等待配置门期间权限撤销均不得读取权威快照、写 SQLite 或触碰 runtime；SQLite 提交失败不得应用 runtime；创建、启动或停止失败后数据库与原任务组合均恢复；双重回滚失败明确暴露；绑定保存不得穿插同设备硬件/IO 或模板重置；并发绑定保存按 PLC 串行、其它 PLC 不受阻；离线保存显示等待 PLC。 |
| GAP-PLC-015 | `PLC-BIND-004`、`PLC-BIND-005` | B05-A 已按 `TaskKey` 计算并应用增量，单任务句柄拥有独立取消源；启停目标任务不再重建 PLC service、连接、buffer、基础任务或其它业务任务。B05-B 又禁止 `TaskBindingSave` 通过旧 `PlcRuntimeApplyService` 触发整机重载，绑定变化只能进入集成事务。 | 源码具备候选证据，Windows/PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcDeviceRuntimeHandle.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcLifecycleCoordinator.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcTaskBindingTransactionService.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcRuntimeApplyService.cs` | 保持绑定入口只应用 `TaskKey` 增量，禁止恢复整机 `ReloadAsync` 或运行时独立入口；取得真实 Windows、PLC 和用户确认前保留本行。 | 分别启停 MG1、MG2 时只有目标任务句柄变化；TCP 连接、PLC service、buffer、其它任务、上下文和其它 PLC 的实例引用保持不变。 |
| GAP-PLC-016 | `PLC-BIND-003`、`PLC-CONN-002` | B05-A 已把连接能力与业务任务生命周期拆开：首次只启动连接/重试，连接成功后按已注册计划逐任务启动；断联只暂停既有业务任务，保留 runtime、PLC service、buffer、任务实例和业务上下文，重连后同一实例只恢复一次。离线期间保存的新绑定由当前计划保留，后续连接成功时启动一次。 | 源码具备候选证据，Windows/PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcLifecycleCoordinator.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcDeviceRuntimeHandle.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcTaskBindingTransactionService.cs`；对应 Runtime Workflow 测试 | 保持连接前零业务循环、断联暂停和同实例单次恢复；取得真实 Windows、PLC 和用户确认前保留本行。 | 初始不可达时业务任务零循环；连接后各启动一次；断联时实例与上下文不被销毁；重连后恢复原实例且不重复启动；离线新增绑定只在后续连接成功时启动一次。 |
| GAP-PLC-017 | `PLC-TASKSTATE-001` | B09 已增加进程内单例 `PlcTaskRuntimeStatusStore`，以大小写不敏感的 `PlcCode + TaskKey` 维护等待 runtime/连接、启动、运行、停止和故障状态，不使用 `DeviceName` 或数据库行号且不新增 SQLite 表。B09-R 又补入 `LastSuccessfulAtUtc`：只在任务成功完成启动/恢复握手并进入 `Running` 时写入，首次成功前为空，故障、停止和等待连接时保留，重连成功后推进；绑定 DTO 和页面行模型投影该值，并明确显示为“最近成功启动/恢复”。任务计划同时按候选声明的 `Read` 信号及其已解析 IO 类别显式记录读依赖，只有周期读取实际负责的单点读/连续读数据类别才标记，交互读不误标；周期读取非 transport 故障或意外退出时，只暂停当前 PLC 的真实读依赖任务并保留 write-only/交互读任务、任务实例和稳定上下文。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Common/Plc/PlcTaskRuntimeStatus.cs`；`IIoT.EdgeClient/src/Application/IIoT.Edge.Application/Features/Hardware/PlcTaskBindings/PlcTaskBindingModels.cs`、`PlcTaskBindingService.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcRuntimeTaskBinder.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcRuntimeTaskPlan.cs`、`PlcDeviceRuntimeHandle.cs`、`PlcRuntimeTaskController.cs`、`PlcLifecycleCoordinator.cs`；`IIoT.EdgeClient/src/Presentation/IIoT.Edge.Presentation.Navigation/Features/Hardware/PlcTaskBindingView/ViewModels/PlcTaskBindingViewModel.cs`；对应 Application、Runtime Workflow、Shell UI 测试 | 保持状态唯一键、配置优先级、最近成功启动/恢复的精确定义、实际 read owner 归属、读依赖显式计划、同实例重连、隔离快照和页面定向订阅；在 Windows/真实 PLC 上完成并行任务、周期读取故障、断联重连、停止超时、设备改名和安全清理验收，取得用户确认前不得关闭。 | 同一 PLC 可同时准确显示真实读依赖任务故障和 write-only/交互读任务运行；周期读取非 transport 故障不伪造断联且不会忙循环重启；最近成功启动/恢复在故障与等待期间保留、真实重连成功后推进；断联后有效任务等待连接并复用原实例；停止失败保留具体错误码；页面停用后不再接收更新；真实 Windows/PLC 与用户确认另行完成。 |
| GAP-PLC-018 | `PLC-CONN-001` | `PlcIoScanTaskBase.ConnectAsync` 已以 TCP `ConnectAsync=true` 作为初始连接门禁并立即 `MarkConnected`；周期读取只能刷新成功时间，不能自行提升连接；纯写任务不发读取探针。B02 的行为测试、Architecture/Security 与 review 已通过，连接门禁本身已有关闭证据；剩余 transport、MC 协议、报文、配置和任务异常分类只归 `GAP-PLC-019`。 | 具备关闭证据，待用户确认 | `PlcIoScanTaskBase.cs` 的 `ConnectAsync`、`ExecuteOneCycleAsync`；`PlcConnectionStatusStore.cs` 的 `MarkConnected`、`MarkProtocolSuccess`；`PlcIoScanTaskBehaviorTests.cs`；Edge B02 merge `54c4380a1fa050c87b44b90b01d150141e1ed7b6` | 保持现有 TCP 门禁和纯写零 probe 行为；异常分类后续只更新 `GAP-PLC-019`。取得用户确认前保留本行。 | TCP 成功立即连接；纯写零 read probe；周期读取不提升连接；B02 受影响验证和 review 证据可追溯；用户确认关闭裁决。 |
| GAP-PLC-019 | `PLC-ERROR-001`、`PLC-CONN-002`、`PLC-WRITE-FAIL-001` | B09 已将 PLC 异常结构化为 `TransportDisconnected`、`Timeout`、`ProtocolRejected`、`InvalidResponse`、`ConfigurationInvalid`、`TaskFault`，遍历包装异常和 `AggregateException` 且显式 `SocketException` 优先。只有 transport 分类会 `MarkDisconnected`、释放连接并暂停依赖任务；超时、MC 协议拒绝、坏报文、非法地址、裸 `IOException`、未知异常和非调用方取消均不冒充断联，读失败发布稳定失败质量，写失败保留原意图。B09-R 补正周期读取观察器：非 Socket 故障或意外正常退出在 per-runtime 门内只停止 `RequiresPeriodicRead=true` 的任务，保留 TCP、PLC service、buffer、任务实例、上下文和 write-only 任务；成功暂停写入 `PeriodicReadFault`，停止超时或失败则保留 `TaskStopTimeout`/`TaskStopFailed`。只有真实 transport 重连或受控 runtime 重载成功启动周期读取后才恢复读依赖任务，不自动忙循环重启。日志、状态和 UI 仍只投影稳定原因码、安全文案及异常类型。 | 源码候选满足，Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Signals/PlcOperationFailureClassifier.cs`、`PlcIoScanTaskBase.cs`、`PlcDataReadScanTask.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.DeviceComm/Plc/PlcRuntimeTaskPlan.cs`、`PlcDeviceRuntimeHandle.cs`、`PlcLifecycleCoordinator.cs`、`PlcConnectionStatusStore.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.Bootstrap/Core/PlcRuntimeTaskBinder.cs`；对应 Runtime Workflow、Application、Shell UI 测试 | 保持 Socket-only 断联、非 transport 失败质量/写意图、周期读取读依赖定向暂停、停止错误码优先、调用方取消传播、隔离路径和脱敏诊断；在 Windows/真实 PLC 上完成 Socket 关闭、周期读取意外退出、超时、MC 错误、坏报文、非法地址和任务故障验收，取得用户确认前不得关闭。 | Socket 关闭进入重连；周期读取非 Socket 故障保留 TCP 且只暂停读依赖任务，write-only 与其它 PLC 继续；停止超时/失败不被 `PeriodicReadFault` 覆盖；超时、MC 协议错误、坏报文、非法地址和裸 `IOException` 不触发连接释放；日志/UI 无原始异常消息；真实 Windows/PLC 与用户确认另行完成。 |
| GAP-PLC-020 | `PLC-READ-FAIL-001`、`PLC-BUFFER-001`、`PLC-QUALITY-001`、`PLC-CLIP-002` | block 或按需读取失败只发布类型默认值、`ReadSucceeded=false`、共同批次和失败时间；last-good 只保留在诊断维度。SDK 强类型快照访问器对缺失或失败信号返回不可用，不回退实时 Buffer；AP/CP 在缺失、混批、失败质量、过期、断联或任务重建时调用 `MarkUnavailable`、清空空码确认，且不解码、不推进、不入队、不完工。三个不同且连续成功的新鲜 Generation 才能确认空弹夹。B04 历史候选已有兼容证据；B16 当前源码候选尚未运行 exact-SHA。 | 源码候选满足；B16 exact-SHA、真实 Windows/PLC 和用户确认未完成 | `IIoT.Edge.Sdk/src/IIoT.Edge.Module.Sdk/Signals/PlcReadSnapshotAccessor.cs`；Edge `PlcBuffer.cs`、`PlcBusinessOnDemandReadCoordinator.cs`；AP/CP `*ClipScanTask.cs`；B04 兼容证据 `IIoT.Edge.Sdk/artifacts/compatibility/b04r-rerun2-host-33db59d4-sdk-0285df83-plugins-16070216.json`；B16 当前 merge 见本文件第 1 节 | 保持失败质量零业务 fallback、last-good 仅诊断、按需/周期结果不混批和三次不同新鲜 Generation 完工门；B17 exact-SHA、真实 Windows/PLC 与用户确认完成前保留本行。 | 失败轮默认值不进入业务；同一失败或重复 Generation 不累计空码；中间失败或过期立即清零；三个不同成功新鲜 Generation 才完工；B17 exact-SHA 与真实 Windows/PLC 另行验收。 |
| GAP-PLC-021 | `PLC-BIND-006` | B10/B10-R 已形成按大小写不敏感 `ModuleId + PlcCode + TaskKey` 独立保存的检查点与 revision 确认契约，并固定“先停止目标任务产生新状态，再原子保存检查点”的停用顺序及“停止任务并保存检查点 → 释放 runtime → 最终保存上下文”的关闭顺序；失败必须回滚或聚合传播。B10-S 把同一原子成功新鲜快照中的弹夹码、数量、速度和采集 UTC 保存为完整现场读值，三项业务读值变化会推进 revision，采集 UTC 单独推进不会产生落盘抖动。B10-T 将 AP/CP 提升为 `2.0.17`，各自按大小写不敏感的 `PlcCode + TaskKey` 隔离确认租约，绑定当前 revision、最近成功新鲜快照时间和配置新鲜窗口；租约缺失、revision 不一致或超窗时以 `ObservationNotEligible + RecoveryObservationUnavailable` 失败关闭，零检查点修改、零审计、零业务输出。断联、缺失/混批/失败/过期快照、解码异常和任务停止会先撤销租约；首次失效清空可确认观测、只推进一次 revision 并保存稳定原因码，重复失效不持续写盘。失效持久化失败时内存租约仍撤销，重启后默认无租约；下一份成功新鲜快照必须先保存完整观测并推进 revision，即使三项与失效前相同，保存成功后才签发新租约；连续健康且三项不变只刷新运行期新鲜时间。断联、重连和周期读取暂停仍复用原实例且不创建恢复检查点。B16 保持上述契约不变，当前源码候选已推进到 Host `2.0.14`、SDK `2.0.11`、AP/CP `2.0.19`；未运行 B17 exact-SHA、Prepare、发布或部署，Published 仍为 Host `2.0.11`、AP/CP `2.0.13`。受保护原文继续保持原字节，本项不增加迁移计数。 | 源码候选满足；B16 exact-SHA、Windows/真实 PLC/用户确认未完成 | `IIoT.EdgeClient/docs/客户端规则.md` 的 `PLC-BIND-006`；B10-T exact-SHA 证据 `IIoT.Edge.Sdk/artifacts/compatibility/b10t-host-bea25510-sdk-45094fe1-plugins-1b9ec10c.json` 为 `passed`（SHA-256 `280f2663f55433b0e1892e0e0c60750520ec1cf2b915edcc7dcd40be1fc2142f`，11,404 字节）；B16 当前 merge 见本文件第 1 节，尚无 B17 exact-SHA 证据 | 保持检查点身份、停用事务、关闭顺序、失败传播、完整现场观测、确认租约、同码续跑、异码/空码确认、审计终止零业务输出、revision 并发门及 MG1/MG2 隔离；取得 B17 exact-SHA、Windows、真实 PLC 和用户确认前保留本行，不得标记已安装、已运行或已投产。 | MG1/MG2 租约互不影响；断联、读取失败、混批、过期、停止、超窗和重启后旧 revision 均不可确认；失效保存失败仍撤销租约，重复失效不产生 revision 抖动；下一份同值新鲜快照必须持久化为新 revision 后才可确认；保存失败完整回滚观测、revision 与审计；审计零完工、零 MES/Cloud、零产量；B17 exact-SHA 和真实 Windows/PLC 另行验收。 |
| GAP-PLC-022 | `PLC-STATUS-001` | AP/CP 仍只有 MG1、MG2 候选且没有 R100 映射或独立设备状态事实。Host 已能识别 `DeviceStatus` 场景，但专用业务接口缺失时 `CloudConsumer` 只阻断普通过站；`CloudRetryRecordProcessor` 只保留 pending，尚无 R100 → Cloud-only durable → 专用接口真实接收闭环。独立的 EdgeHost PLC runtime 状态快照不是这条生产设备状态事实。内部规则虽已记录 `R100 / Int16 / 1 word`、`-1/0/1/2/3` 与 Cloud-only 候选口径，但 PLC 方尚未正式确认，且本批未授权修改 Cloud；因此延期到后续 Cloud 专项，不得实现半条链路。 | 未满足 | `IIoT.EdgeClient/docs/客户端规则.md` 的 `PLC-STATUS-001`；AP/CP `*StationRuntimeFactory.cs`、`*PlcSignals.cs`；`IIoT.EdgeClient/src/Infrastructure/IIoT.Edge.Infrastructure.Integration/PassStation/CloudConsumer.cs`；`IIoT.EdgeClient/src/Edge/IIoT.Edge.Host.DataPipeline/Services/CloudRetryRecordProcessor.cs`；`EdgeHostPlcRuntimeStateReporter.cs` | 取得 PLC 方对现有 R100 地址、类型和状态语义的正式确认并另行授权 Cloud 专项后，增加第三任务和 R100 映射，只形成 Cloud 目标事实，再接入专用 durable/retry/deadletter 与 Cloud 接口；禁止进入 MES、普通过站或生产统计。 | R100 有效值产生可追溯 Cloud-only 事实；MES 零调用；关闭、离线、失败和第 20 次失败进入正确状态；只有真实接收后成功。 |
| GAP-PLC-023 | 全部 62 条受保护原规则 | B02 已按原字节恢复 `业务规则.md`；B03—B10 迁入 25 条，B11-A 迁入 Host/Launcher 9 条与 PLC 7 条，B11-B 迁入 Cloud/MES 21 条，现已完成 62/62。完整 62 行矩阵保留功能真实状态及受控替代说明；正式规则另有新增 `HL-RELEASE-001`。原文仍保持 712 行、53,376 字节及固定 SHA-256，迁移完成不等于允许删除。 | 迁移 62/62，原文删除待用户确认 | 恢复 commit `15457cfed9e89b98123c6a3191cc2f39ea847d4b`；`IIoT.EdgeClient/docs/业务规则临时和待后续处理问题/业务规则.md`；`IIoT.EdgeClient/docs/客户端规则.md`；本文件 1.1 节矩阵；编号唯一性、矩阵、全工作区引用和恢复 hash 扫描。tracked 引用仅另保留 Edge `AGENTS.md` 的删除门路由；Deployment 活动文档仍指向 Edge `e84b1f1eac474cdca43ccf46d941e9777442f277` 并保留 `25/62` 旧口径，本批按授权禁止修改 Deployment，因此该行不能作为当前迁移进度源 | 保持本 GAP 开放和原文原字节；只有两个文档 PR 的 review、合并与清理均完成，并再次确认恢复 commit/hash、活动引用和无相反语义后，才允许由用户明确决定是否另批删除原文。 | 正式文档包含原始 62 个编号各一次，并另有新增 `HL-RELEASE-001`；矩阵 62/62、无漏项/重复/死目标，所有受控替代有解释；删除前六项门禁齐全并取得用户明确确认。 |

## 9. 本批后续统一处理顺序

1. B12—B16 的非 Cloud 客户端功能实现基线已按本文件第 1 节收口；当前停止在源码与受影响测试证据，不得描述为 exact 候选已冻结、已安装、已运行、已投产或当前 catalog 已发布。
2. 下一步只能在用户另行授权 `IIoT.Deployment` 和指定 Windows/PLC 窗口后进入 B17：先同步 Deployment 当前事实，再运行 exact-SHA 与 `Validate-Candidate`，通过后才能 `Prepare-Release`；Windows 候选只能使用 prepared 内嵌字节。
3. 现场验收通过前禁止 catalog Deploy；后续正式发布仍需独立生产写入授权，且 catalog 发布、Windows 安装和现场投产必须分开记录。
4. `GAP-HL-002` 的 Cloud 首装重复绑定载体、`GAP-PLC-013/022` 的第三任务/R100/Cloud 专用接口，以及 PASS/CAP/DASH 的真实 Cloud 联合验收继续延期到后续 Cloud—客户端对接窗口；当前禁止写 Cloud 或 AICopilot。
5. `GAP-PLC-023` 已记录迁移 62/62，但原文删除仍等待用户最终确认，不得关闭；配方业务继续暂停，除非用户另行启动专项。
